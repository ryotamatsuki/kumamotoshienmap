# 時点修正更新オペレーション

> Version 2.4（2026-08-24改訂）
>
> Version 2.3までのRelease Gate、対口支援・他自治体支援監査、国・関係機関支援監査に加え、**機関カバレッジ監査（Institution Coverage Gate）**を必須化する。
>
> 固定リストだけ、現在画面に出ているレコードだけ、前回監査に載った機関だけのいずれか一つを対象集合としてはならない。固定最低確認先、現行ダッシュボード、前回監査、最新の政府・熊本県等の横断資料から機関集合を作り、**「確認すべき機関集合 − 裁定済み機関集合 = 0件」**を確認してから時点修正を完了する。

## 1. 目的

熊本県への支援・受援ダッシュボードを、公式情報の対象時点と定義を壊さずに更新し、GitHub PagesおよびSites用ビルドへ再現可能な形で反映する。

対象リポジトリ：`ryotamatsuki/kumamotoshienmap`

完了とはファイル更新ではなく、次の全条件を満たすことをいう。

1. 最新mainを基準に更新する。
2. 公式情報の対象時点・公表日時・取得日時・確認日時を分離する。
3. CURRENT / HISTORICAL / PLANNED / UNKNOWN / CONFLICT / NOT_APPLICABLEを区別する。
4. 45市町、対口支援・他自治体支援、国・関係機関、全国組織を漏れなく再監査する。
5. 機関カバレッジ集合の差分が0件であることを確認する。
6. source / public / dist / Pages実配信の整合を確認する。
7. candidate生成、validator、build、Release Gate、CIを通過する。
8. merge後にPages実配信とDesktop/Mobileを確認する。

## 2. 時点の定義

| フィールド | 意味 |
|---|---|
| reference_at | 今回の更新判断の基準日時（JST） |
| source_as_of | 公式資料が表す対象時点 |
| source_published_at | 資料の公表日時 |
| source_updated_at | 公式ページ・JSONの更新日時 |
| fetched_at | API・JSON取得日時 |
| checked_at | 担当者が内容を確認した日時 |
| page_checked_at | ページ全体を最後に実機確認した日時 |
| release_id | `YYYYMMDD-HHMM`形式の更新識別子 |
| deployed_sha | 公開反映後のmain SHA |

これらを相互に代用しない。

## 3. 状態管理

| 状態 | 意味 |
|---|---|
| CURRENT | 基準日時点で適用中と確認済み |
| HISTORICAL | 終了済み・過去実績 |
| PLANNED | 公式予定だが実施・実働未確認 |
| UNKNOWN | 公式情報不足・取得失敗等で確認不能 |
| CONFLICT | 公式情報間で対象時点・定義・値が相違 |
| NOT_APPLICABLE | 公式に対象外、または今回の表示対象外と確認済み |

禁止事項：

- 404を「募集なし」「終了」に変換しない。
- フォーム終了を「定員到達」に変換しない。
- 予定人数を実績人数に変換しない。
- 個人募集から団体受入れを推定しない。
- 県報件数、ライブJSON件数、位置履歴件数を同一指標として扱わない。
- 未公表値を0や推計値へ変換しない。
- 古い人数・台数・団体数に「要確認」とだけ付けてCURRENT表示を継続しない。
- 新しい公表がないことだけを根拠にHISTORICALへ移さない。

## 4. 主要ファイルの役割

| 種類 | ファイル |
|---|---|
| Pages入口 | `index.html` |
| Pages本体・正本 | `ehime_kumamoto_support_geocoded_shelters_20260802.html` |
| Sites公開コピー | `public/dashboard.html` |
| 現在避難所 | `current-shelters.json` |
| 座標履歴 | HTML内座標マスター / `shelter-coordinate-manifest.json` |
| ボランティア調査 | `research_official_*.json` |
| ボランティア生成物 | `volunteer-data.js` |
| 対口・自治体監査 | `municipal-support-audit.json` |
| 国・関係機関監査 | `national-support-audit.json` |
| UI | `volunteer.js` / `volunteer.css` |
| Release Gate | `scripts/validate-update-release.mjs` |
| Gate自己テスト | `scripts/test-release-gate.mjs` |
| 台帳schema | `scripts/update-ledger.schema.json` |
| CI | `.github/workflows/validate-update.yml` |
| 公開後QA | `.github/workflows/post-deploy-pages-smoke.yml` |

`update-*202608*.mjs`、`fix-*202608*.mjs`、`cleanup-*202608*.mjs`等の過去更新用migration scriptは、新しい時点へ無検証で再実行しない。一時workflow・一時migrationは最終PRから必ず除去する。

## 5. 更新開始

```sh
git fetch origin
git status --short
git log -1 --oneline origin/main
git switch -c audit/timepoint-YYYYMMDD-HHMM origin/main
```

古い参考SHAへresetしない。未コミット変更を破棄しない。同一ファイルを複数エージェントが並列編集しない。作業開始時main SHAを台帳とPR本文へ記録する。

## 6. UPDATE_LEDGER schema v2

実更新PRでは、台帳をGit管理する。

推奨保存先：`operations/ledgers/refresh-YYYYMMDD-HHMM.json`

最低限の構造：

```json
{
  "schema_version": 2,
  "repository": "ryotamatsuki/kumamotoshienmap",
  "update_type": "timepoint_refresh",
  "gate_phase": "pre_merge",
  "update_id": "refresh-YYYYMMDD-HHMM",
  "release_id": "YYYYMMDD-HHMM",
  "reference_at": "YYYY-MM-DDTHH:MM:SS+09:00",
  "page_checked_at": null,
  "base_main_sha": "40文字SHA",
  "expected_changed_files": [],
  "sources": [],
  "coverage": {},
  "snapshots": {},
  "blocking_unresolved": [],
  "accepted_unresolved": []
}
```

### 6.1 blocking_unresolved

公開・mergeを止める未解決事項。1件でも存在すればRelease GateはFAILする。

例：現行件数が定義上確定できない、座標conflictを解消できない、current/historyの裁定が表示を誤らせる可能性がある、機関カバレッジに未裁定がある、source/public/dist不一致原因が不明、など。

### 6.2 accepted_unresolved

UNKNOWN、CONFLICT、PLANNED等を「未確認であること自体を明示した状態」で安全に公開可能な場合に使用する。

各項目に以下を必須とする。

- `issue_id`
- `state`: `UNKNOWN` / `CONFLICT` / `PLANNED` / `OTHER`
- `reason`
- `owner`
- `source_ids`
- `next_review_at`

`next_review_at`は`reference_at`より後、`source_ids`は台帳`sources`登録済みとする。acceptedを使って不都合な事実を隠してはならず、表示上もUNKNOWN/CONFLICT等を維持する。

### 6.3 coverageに残す機関カバレッジ証跡

各timepoint refreshでは、少なくとも次を`coverage.institution_audit`相当として記録する。schema実装が未対応の場合でもPR本文に同じ情報を記載する。

```json
{
  "institution_audit": {
    "fixed_minimum_checked": true,
    "dashboard_inventory_count": 0,
    "previous_audit_inventory_count": 0,
    "official_discovery_count": 0,
    "required_entity_count": 0,
    "adjudicated_entity_count": 0,
    "missing_entities": [],
    "newly_discovered_entities": []
  }
}
```

**`missing_entities`が1件でもあればmerge不可。** 現在のvalidatorがこの配列を機械検査しない場合でも、PRレビュー上のblocking条件として扱う。

## 7. expected_changed_files

台帳の`expected_changed_files`と実PR差分は完全一致させる。不足・想定外ファイルはいずれもFAIL。

`dist/`がGit非管理の場合はexpected_changed_filesへ入れず、`npm run build`と`validate:dist`で生成物を検査する。

```sh
npm run validate:release -- \
  --ledger=operations/ledgers/refresh-YYYYMMDD-HHMM.json \
  --base=origin/main \
  --require-ledger-if-data-change
```

## 8. 公式情報の再探索

優先順位は、被災自治体・熊本県・社協等の公式Web/PDF/JSON、派遣元自治体・愛媛県、国・政府機関、全国組織の公式情報、公式SNS・公式フォーム、二次情報の順。二次情報は一次情報発見用途に限定する。

45市町村を必ず`rechecked_current / rechecked_no_change / not_applicable / official_not_found / unknown_needs_recheck / conflict`等へ分類する。未確認項目を推定しない。

### 8.0 機関カバレッジ監査（最優先・必須）

個別データを更新する前に、今回確認すべき「機関集合」を確定する。**前回のレコード一覧だけを再確認して終了してはならない。**

#### 8.0.1 required_entitiesの作り方

`required_entities`は次の和集合とする。

1. **固定最低確認先**：下記8.0.2の機関・組織群。
2. **現行ダッシュボード集合**：HTML/JS/JSON上のprovider、record、source、支援主体、出典機関名を機械抽出した全機関。
3. **前回監査集合**：`municipal-support-audit.json`、`national-support-audit.json`、直前UPDATE_LEDGERの全機関。
4. **横断公式資料からの新規発見集合**：最新の内閣府・熊本県等の災害対応状況、各省庁横断資料、対口支援名簿等に新たに登場した機関。
5. **表示外だが現況に関係する新規機関**：今回の探索で新規発見した機関。表示採用しない場合もNOT_APPLICABLE/UNKNOWN等の裁定を残す。

機関名の表記ゆれはcanonical IDへ寄せる。ただし、本省と地方支分部局、全国組織と地方支部が別の事実を公表している場合は別entityとして保持する。

完了条件：

```text
required_entities - adjudicated_entities = ∅
```

また、次も確認する。

```text
current_dashboard_entities ⊆ required_entities
previous_audit_entities ⊆ required_entities
officially_discovered_entities ⊆ required_entities
```

#### 8.0.2 固定最低確認先

以下は「現行表示にあるか否か」にかかわらず毎回の探索起点とする。関与なしを確認した場合はNOT_APPLICABLE等で明示する。

**A. 被災地側**

- 熊本県（災害対策本部・各所管の最新被害/支援情報）
- 熊本県内45市町村（熊本市を含む）
- 熊本県社会福祉協議会
- 各市町村社会福祉協議会（ボランティア情報を扱う場合）
- 熊本県内の医療・福祉・職能団体等、ダッシュボードまたは横断資料に登場する地域機関

**B. 愛媛県側**

- 愛媛県（本庁、派遣・受援・災害対策関係所管）
- 愛媛県内市町・県社協・関係団体のうち、現行表示・派遣実績・新規横断資料に登場する主体

**C. 対口支援・他自治体支援**

- 熊本県等の対口支援・応急対策職員派遣制度の割当名簿に登場する全派遣元
- 都道府県、市区町村、指定都市、地域ブロック等の全構成団体
- 派遣元自治体の公式発表で追加確認された主体
- 薬剤師会等の地域職能団体・地域機関で現行表示に含まれる主体

**D. 国・政府機関（最低ライン）**

- 内閣府（防災担当）・政府非常災害現地対策本部等
- 総務省（地方行財政・財政支援等）
- 消防庁
- 国土交通省
- 九州地方整備局、近畿地方整備局その他関係地方整備局
- TEC-FORCE
- 厚生労働省
- 防衛省・自衛隊（統合幕僚監部、陸海空の個別情報がある場合は個別確認）
- 警察庁（管区警察局等の個別情報がある場合は個別確認）

**E. 政府横断資料で登場し得る省庁等**

- 農林水産省
- 経済産業省
- 環境省
- 文部科学省
- 法務省
- 財務省・国税関係
- その他、最新の政府横断災害資料・熊本県資料に章・項目・支援主体として登場した省庁、庁、地方支分部局

このE群は「常にCURRENTデータがある」という意味ではない。**新規参画を見落とさないための探索対象**である。

**F. 全国医療・福祉・職能・支援組織**

現行表示または最新横断資料に登場する以下の系統を必ず確認する。

- DHEAT
- DMAT
- DPAT
- DWAT
- 災害支援ナース等の看護支援
- 日本赤十字社
- 日本医師会・JMAT
- JRAT
- 日本薬剤師会等の全国職能団体
- その他、現行nationalレコードまたは最新公式資料に登場する全国組織

#### 8.0.3 機関の「確認済み」の定義

単に検索しただけでは確認済みにしない。各entityについて最低限、次のいずれかの裁定を持たせる。

- CURRENT：基準日時点の活動・値を一次情報で確認。
- HISTORICAL：終了・撤収・活動完了等を公式確認。
- PLANNED：出発・派遣・実施予定のみ確認。
- UNKNOWN：同一定義の基準日時点を確認不能。
- CONFLICT：複数一次情報が競合。
- NOT_APPLICABLE：今回の対象外・関与なしを合理的に確認。

「検索結果なし」「新着なし」は裁定ではない。原則UNKNOWNまたはNOT_APPLICABLEの根拠を別途残す。

#### 8.0.4 新規機関の取り扱い

前回監査になく今回初めて公式資料で発見した機関は、表示採否を判断する前に`newly_discovered_entities`へ記録する。表示に追加しない場合も理由と状態を残す。

逆に、前回存在した機関を今回の資料で見つけられなくても監査対象から削除しない。終了を確認できなければUNKNOWNとする。

### 8.1 対口支援・他自治体支援の全件再監査（必須）

全ての`timepoint_refresh`で、愛媛県以外の自治体支援も独立した更新対象として再確認する。対象は`provider: municipal`、対口支援団体、熊本県・県内市町村応援、指定都市、地域機関、地域ブロック表示等。

監査結果は`municipal-support-audit.json`へ保存し、`reference_at`を`PAGE_RECHECK_META.checkedAt`と一致させる。ファイル欠落、時点が古い、旧名簿の構成団体に未裁定がある場合は完了扱いにしない。

必須ルール：

- 過去の対口支援「割当名簿」はHISTORICALな配置履歴。名簿掲載だけで現在活動中とみなさない。
- 旧名簿の全構成団体を1団体ずつ再探索し、CURRENT / HISTORICAL / PLANNED / UNKNOWN等へ裁定する。
- 派遣期間・次陣予定だけならPLANNED。期間内であることだけでCURRENTへ昇格しない。
- 終了・撤収・支援先切替を公式確認した場合のみHISTORICALへ移す。
- 新しい一次情報を取得できなければUNKNOWNとし、古い値を現況表示に残さない。
- 404、検索結果なし、新しい発表なしを「終了」と解釈しない。
- 中央集計と派遣元個別公表は対象時点・定義が異なり得るため無理に一致させない。
- `SUPPORT_BLOCKS`等は旧割当履歴であることを明示し、現在活動中団体数として表示しない。

```sh
npm run sync:municipal-support
npm run validate:municipal-support
```

### 8.2 国・関係機関支援の全件再監査（必須）

全ての`timepoint_refresh`で、`provider: national`および画面上「国・関係機関」に分類する全国組織を全件再監査する。

対象は8.0のrequired_entitiesからnational/全国組織を抽出した集合とする。**現行ダッシュボードの12件、13件など固定件数を次回監査の母数にしてはならない。** 件数は毎回再抽出する。

監査結果は`national-support-audit.json`へ保存し、`reference_at`と`release_id`をページ・UPDATE_LEDGERと一致させる。`scripts/sync-national-support-audit.mjs`を生成経路、`scripts/validate-national-support-audit.mjs`を通常buildとRelease Gateの双方から実行する。

必須ルール：

- 古い資料の値を「最新要確認」「継続要確認」だけでCURRENT表示しない。確定できなければUNKNOWN、旧値はHISTORICAL snapshotとしてのみ保持する。
- 派遣予定・出発予定・予定期間内であることだけでCURRENTへ昇格しない。実働確認前はPLANNED。
- 最新発表がないこと、404、検索結果なしを終了根拠にしない。
- 過去累計値と現在活動人数を同一指標として扱わない。TEC-FORCE等は現時点人数と累計人日を分離する。
- 省庁ごとに`source_as_of`が異なる値を同一時点の全国集計のように表示しない。
- CURRENTの最終根拠は原則一次情報。二次情報は発見・クロスチェック用途に限定する。
- 愛媛県由来は愛媛県支援、自治体由来はmunicipalへ分離し、nationalへ二重計上しない。
- 厚労省の全国保健医療福祉、警察庁の活動、総務省の財政支援等、値が取得しにくい機関も監査対象から外さずUNKNOWNとして残す。

```sh
npm run sync:national-support
npm run validate:national-support
```

### 8.3 機関カバレッジの最終照合

個別監査後に、次を必ず実施する。

1. 現行HTML/JS/JSONから機関名・provider・record ID・source IDを再抽出する。
2. municipal監査の全entityを列挙する。
3. national監査の全entityを列挙する。
4. 最新横断公式資料で新規機関がないか再確認する。
5. required_entitiesとadjudicated_entitiesの集合差分を確認する。
6. 差分があればblocking_unresolvedへ入れ、mergeしない。

PR本文には最低限、次を明記する。

```text
Institution coverage:
- required_entities: N
- adjudicated_entities: N
- missing_entities: 0
- newly_discovered_entities: N
```

「前回と同じ機関しかなかった」場合も`newly_discovered_entities: 0`と明記し、探索を省略して0とみなさない。

## 9. 現在避難所 candidateフロー

本番ファイルへ直接書く前にcandidateを生成する。

```sh
npm run update:current-shelters -- \
  --dry-run \
  --candidate-output /tmp/current-shelters-candidate.json

node scripts/validate-current-shelters.mjs \
  --candidate=/tmp/current-shelters-candidate.json
```

candidateではschema、`meta.current_count`と配列件数、facility ID、municipality、緯度経度、source metadata、unresolved、coordinate conflict、座標マスター整合を確認する。

PASS後のみ本番`current-shelters.json`へ反映する。公式JSONの現行定義は`startあり / endなし`。履歴座標を現行件数へ足し戻さない。100m超座標差は自動採用しない。

## 10. source / public / dist

正本は原則source HTML。

```sh
cp ehime_kumamoto_support_geocoded_shelters_20260802.html public/dashboard.html
npm run generate:current-shelter-data
npm run sync:municipal-support
npm run sync:national-support
npm run sync:page-metadata
cmp --silent ehime_kumamoto_support_geocoded_shelters_20260802.html public/dashboard.html
```

`sync:page-metadata`は`PAGE_RECHECK_META.checkedAt`を正として表示時刻を同期する。確認時刻を個別にhard-codeしてはならない。

`npm run build`後はsource/public、dist、Sites Worker、index参照、現況時刻、municipal/national overlay等をvalidatorで確認する。

## 11. release_id / cache bust

`volunteer-data.js?v=<release_id>`をsource/public/dist/Workerで一致させ、台帳`release_id`とも一致させる。validator内の旧release_id残存も検査する。

## 12. validator分類

時点依存：`validate:dashboard`、`validate:current-state`、`validate:volunteer`、`validate:municipal-support`、`validate:national-support`、`validate:dist`

構造・品質：`validate:shelters`、`validate:current-shelters`

Release Gate：`validate:release`

Release Gate自己テスト：`test:release-gate`

固定本数表現は使用しない。

## 13. Release Gate自己テスト

`npm run test:release-gate`は実validatorへ故障を注入してFAIL条件が機能することを確認する。

最低限の検査：

1. source/public不一致 → FAIL
2. release_id不一致 → FAIL
3. expected_changed_files不一致 → FAIL
4. candidate count不一致 → FAIL
5. coordinate conflict → FAIL
6. 画面上現況確認時刻と`PAGE_RECHECK_META`不一致 → FAIL
7. current metadata旧時点 → FAIL
8. HISTORICALの過去日付 → 誤検出せずPASS
9. accepted_unresolved適正記録 → PASS
10. blocking_unresolvedあり → FAIL
11. municipal監査欠落・旧時点 → FAIL
12. national監査欠落・旧時点 → FAIL
13. national既存レコード未裁定 → FAIL
14. UNKNOWNの旧snapshotをcurrent表示 → FAIL

機関カバレッジ集合差分をvalidatorへ実装した場合は、`missing_entities > 0 → FAIL`の自己テストも追加する。未実装の間も文書運用上はmerge blockerとする。

## 14. GitHub Actions

main向けPRで`.github/workflows/validate-update.yml`を実行する。通常CIはchecked-inデータとfixtureを検証する。

CI実行内容：Nodeセットアップ、`npm run build`、build非書換確認、JS syntax check、`npm run test:release-gate`、`git diff --check`、`npm run validate:release -- --base=<PR base> --require-ledger-if-data-change`。

データ変更があるのに台帳がない場合はFAIL。

## 15. Branch Protection / Ruleset

推奨必須設定：

- mainへのPR経由を必須
- `Build and release gate`成功を必須status check化
- merge前にbranchを最新mainへ追随
- mainへの通常直接pushを禁止
- 管理者bypassは緊急時のみ

設定変更権限/APIが利用できない環境では、未設定を「完了」と報告しない。

## 16. PR前ローカルゲート

```sh
npm run build
npm run test:release-gate
npm run validate:municipal-support
npm run validate:national-support
node --check volunteer.js
node --check volunteer-data.js
node --check scripts/build-sites.mjs
node --check scripts/validate-update-release.mjs
node --check scripts/test-release-gate.mjs
node --check scripts/sync-national-support-audit.mjs
node --check scripts/validate-national-support-audit.mjs
git diff --check
npm run validate:release -- --staged --ledger=<ledger-path>
```

加えて、機関カバレッジについて`missing_entities = []`を目視またはスクリプトで確認する。

## 17. PR / merge

PR本文に以下を記録する。

- 開始時main SHA
- reference_at / release_id
- 主要sourceとsource_as_of
- fixed minimum確認結果
- required/adjudicated/missing/newly discovered entity数
- municipal/national監査結果
- expected_changed_files
- blocking/accepted unresolved
- candidate validation
- build/self-test/Release Gate
- source/public/dist一致
- 残存リスク

CI成功後にhead SHAを固定してmergeする。headが動いた場合は再確認する。

## 18. 公開後QA

merge後main SHAを記録し、Pages実配信後にroot、本体HTML、CSS/JS/JSON、Desktop/Mobile、6ビュー、出典リンク、地図、Console、CURRENT/HISTORICAL/PLANNED/UNKNOWN表示、release_id/cache、national/municipal overlayを確認する。

`.github/workflows/post-deploy-pages-smoke.yml`でPages上の主要ファイルとmerged mainの一致、現況確認時刻、release_id、Desktop/Mobile runtime、ビュー、page/console error、重要national表示を自動検査する。workflow PASSを公開後QAの機械的証跡とし、最終目視は補完する。

## 19. 残存する人間判断

完全自動化しない事項：公式情報の意味解釈、PLANNED→CURRENTの裁定、404と終了の区別、異なる集計定義の裁定、accepted_unresolvedが安全かの判断、新規機関の表示採否、Desktop/Mobile最終目視。

人間判断が必要であること自体はエラーではない。推定で埋めず、状態・根拠・次回レビューを記録する。

## 20. 最終ゲート

### 機関カバレッジ

- [ ] 固定最低確認先A〜Fを探索
- [ ] 現行ダッシュボードから全provider / record / source / 機関を抽出
- [ ] 前回municipal/national監査の全entityを継承
- [ ] 最新政府・熊本県等の横断資料から新規機関を探索
- [ ] 新規機関を`newly_discovered_entities`へ記録
- [ ] required_entitiesを確定
- [ ] 全required entityをCURRENT/HISTORICAL/PLANNED/UNKNOWN/CONFLICT/NOT_APPLICABLEへ裁定
- [ ] `missing_entities = []`
- [ ] required_entities件数 = adjudicated_entities件数

### データ・監査

- [ ] 最新mainから開始
- [ ] schema v2台帳をGit管理
- [ ] blocking_unresolved = 0
- [ ] accepted_unresolvedは必須メタデータ完備
- [ ] 45市町の確認状態に未処理なし
- [ ] 対口支援・他自治体支援を旧名簿の全構成団体まで再監査
- [ ] `municipal-support-audit.json`の`reference_at` = `PAGE_RECHECK_META.checkedAt`
- [ ] 国・関係機関・全国組織を全件再監査
- [ ] `national-support-audit.json`の`reference_at` = `PAGE_RECHECK_META.checkedAt`
- [ ] municipal/nationalの`release_id` = ページ/台帳release_id
- [ ] `npm run validate:municipal-support` PASS
- [ ] `npm run validate:national-support` PASS
- [ ] candidate validator PASS

### Build / Release

- [ ] source/public一致
- [ ] release_id一致
- [ ] 現況表示時刻が`PAGE_RECHECK_META`と一致
- [ ] `npm run build` PASS
- [ ] build後にtracked inputを書き換えていない
- [ ] `npm run test:release-gate` PASS
- [ ] `npm run validate:release` PASS
- [ ] expected_changed_filesと実PR差分が完全一致
- [ ] PR CI PASS
- [ ] 一時workflow / migration / fixerがPR差分に残っていない
- [ ] merge前mainが開始時baseから予期せず進んでいないか確認
- [ ] merge時head SHA固定
- [ ] merge後main SHA記録
- [ ] Pages実配信確認
- [ ] Desktop/Mobile・6ビュー確認
- [ ] Console / page errorなし
- [ ] national/municipalの主要表示と公式リンク確認

前回と同種の問題を公開後の追加PRで修正するのではなく、可能な限りPR merge前に機械的またはチェックリストで停止することを本運用の原則とする。