# 時点修正更新オペレーション

> Version 2.3（2026-08-24改訂）
>
> Version 2.2までのRelease Gateに加え、国・関係機関支援の全件再監査を恒久化する。
>
> 2026-08-24の実更新で「愛媛県・被害・避難所は更新した一方、対口支援・他自治体等に8月8～19日の旧スナップショットが残った」ことを受け、対口支援・他自治体支援の全件再監査を全ての時点修正で必須化する。旧割当名簿を現行活動とみなさず、派遣元公式情報ごとにCURRENT / HISTORICAL / PLANNED / UNKNOWNを裁定し、監査ファイルとvalidatorをRelease Gateへ組み込む。

## 1. 目的

熊本県への支援・受援ダッシュボードを、公式情報の対象時点と定義を壊さずに更新し、GitHub PagesおよびSites用ビルドへ再現可能な形で反映する。

対象リポジトリ：`ryotamatsuki/kumamotoshienmap`

完了とはファイル更新ではなく、次の全条件を満たすことをいう。

1. 最新mainを基準に更新する。
2. 公式情報の対象時点・公表日時・取得日時・確認日時を分離する。
3. CURRENT / HISTORICAL / PLANNED / UNKNOWN / CONFLICTを区別する。
4. source / public / dist / Pages実配信の整合を確認する。
5. candidate生成、validator、build、Release Gate、CIを通過する。
6. merge後にPages実配信とDesktop/Mobileを確認する。

## 2. 時点の定義

| フィールド | 意味 |
|---|---|
| reference_at | 今回の更新判断の基準日時（JST） |
| source_as_of | 公式資料が表す対象時点 |
| source_published_at | 資料の公表日時 |
| source_updated_at | 公式ページ・JSONの更新日時 |
| fetched_at | API・JSON取得日時 |
| checked_at | 担当者が内容を確認した日時 |
| page_checked_at | ページ全体を最後に実機確認した日時 |
| release_id | `YYYYMMDD-HHMM`形式の更新識別子 |
| deployed_sha | 公開反映後のmain SHA |

これらを相互に代用しない。

## 3. 状態管理

| 状態 | 意味 |
|---|---|
| CURRENT | 基準日時点で適用中と確認済み |
| HISTORICAL | 終了済み・過去実績 |
| PLANNED | 公式予定だが実施・実働未確認 |
| UNKNOWN | 公式情報不足・取得失敗等で確認不能 |
| CONFLICT | 公式情報間で対象時点・定義・値が相違 |
| NOT_APPLICABLE | 公式に対象外と確認済み |

禁止事項：

- 404を「募集なし」に変換しない。
- フォーム終了を「定員到達」に変換しない。
- 予定人数を実績人数に変換しない。
- 個人募集から団体受入れを推定しない。
- 県報件数、ライブJSON件数、位置履歴件数を同一指標として扱わない。
- 未公表値を0や推計値へ変換しない。

## 4. 主要ファイルの役割

| 種類 | ファイル |
|---|---|
| Pages入口 | `index.html` |
| Pages本体・正本 | `ehime_kumamoto_support_geocoded_shelters_20260802.html` |
| Sites公開コピー | `public/dashboard.html` |
| 現在避難所 | `current-shelters.json` |
| 座標履歴 | HTML内座標マスター / `shelter-coordinate-manifest.json` |
| ボランティア調査 | `research_official_*.json` |
| ボランティア生成物 | `volunteer-data.js` |
| UI | `volunteer.js` / `volunteer.css` |
| Release Gate | `scripts/validate-update-release.mjs` |
| Gate自己テスト | `scripts/test-release-gate.mjs` |
| 台帳schema | `scripts/update-ledger.schema.json` |
| CI | `.github/workflows/validate-update.yml` |

`update-*202608*.mjs`、`fix-*202608*.mjs`、`cleanup-*202608*.mjs`は過去更新のmigration scriptであり、新しい時点へ無検証で再実行しない。

## 5. 更新開始

```sh
git fetch origin
git status --short
git log -1 --oneline origin/main
git switch -c audit/timepoint-YYYYMMDD-HHMM origin/main
```

古い参考SHAへresetしない。未コミット変更を破棄しない。同一ファイルを複数エージェントが並列編集しない。

## 6. UPDATE_LEDGER schema v2

実更新PRでは、台帳をGit管理する。

推奨保存先：`operations/ledgers/refresh-YYYYMMDD-HHMM.json`

これにより各更新の判断根拠を履歴として残す。CIはPR差分からこのパスを自動検出する。互換目的でルートの`UPDATE_LEDGER.json`も認識するが、新規更新では履歴パスを推奨する。

最低限の構造：

```json
{
  "schema_version": 2,
  "repository": "ryotamatsuki/kumamotoshienmap",
  "update_type": "timepoint_refresh",
  "gate_phase": "pre_merge",
  "update_id": "refresh-YYYYMMDD-HHMM",
  "release_id": "YYYYMMDD-HHMM",
  "reference_at": "YYYY-MM-DDTHH:MM:SS+09:00",
  "page_checked_at": null,
  "base_main_sha": "40文字SHA",
  "expected_changed_files": [],
  "sources": [],
  "coverage": {},
  "snapshots": {},
  "blocking_unresolved": [],
  "accepted_unresolved": []
}
```

### 6.1 blocking_unresolved

公開・mergeを止める未解決事項。1件でも存在すればRelease GateはFAILする。

例：現行件数が定義上確定できない、座標conflictを解消できない、current/historyの裁定が表示を誤らせる可能性がある、source/public/distの不一致原因が不明、など。

### 6.2 accepted_unresolved

UNKNOWN、CONFLICT、PLANNED等を「未確認であること自体を明示した状態」で安全に公開可能な場合に使用する。

各項目に以下を必須とする。

- `issue_id`
- `state`: `UNKNOWN` / `CONFLICT` / `PLANNED` / `OTHER`
- `reason`
- `owner`
- `source_ids`
- `next_review_at`

`next_review_at`は`reference_at`より後でなければならず、`source_ids`は台帳の`sources`に登録済みでなければならない。acceptedを使って不都合な事実を隠してはならず、表示上もUNKNOWN/CONFLICT等を維持する。

旧`unresolved`フィールドはschema v2では禁止する。

## 7. expected_changed_files

台帳の`expected_changed_files`と実PR差分は完全一致させる。不足・想定外ファイルはいずれもFAIL。

```sh
npm run validate:release -- \
  --ledger=operations/ledgers/refresh-YYYYMMDD-HHMM.json \
  --base=origin/main \
  --require-ledger-if-data-change
```

## 8. 公式情報の再探索

優先順位は、熊本県・市町・社協等の公式Web/PDF/JSON、愛媛県・国の公式資料、公式SNS・公式フォーム、二次情報の順。二次情報は発見用途に限定する。

45市町村を必ず`rechecked_current / rechecked_no_change / not_applicable / official_not_found / unknown_needs_recheck / conflict`等へ分類する。未確認項目を推定しない。

### 8.1 対口支援・他自治体支援の全件再監査（必須）

全ての`timepoint_refresh`で、愛媛県以外の支援も独立した更新対象として必ず再確認する。対象は、`provider: municipal`の支援レコード、対口支援団体、熊本県・県内市町村応援、指定都市、薬剤師会等の地域機関、地域ブロック表示である。

監査結果は`municipal-support-audit.json`へ保存し、`reference_at`を`PAGE_RECHECK_META.checkedAt`と一致させる。ファイルがない、時点が古い、旧名簿の構成団体に未裁定がある場合はRelease GateをFAILさせる。

必須ルール：

- 熊本県等が公表した過去の対口支援「割当名簿」は`HISTORICAL`な配置履歴として保存する。名簿掲載だけを根拠に現在も活動中とみなさない。
- 旧名簿の**全構成団体を1団体ずつ**再探索し、CURRENT / HISTORICAL / PLANNED / UNKNOWNへ必ず裁定する。追加派遣主体が見つかった場合も同じ状態管理で追記する。
- `CURRENT`は、基準日時点の活動を派遣元・受援側等の公式一次情報で確認できた場合のみ使用する。
- 派遣期間・次陣予定だけが公式公表されている場合、当日の実働を確認できなければ`PLANNED`とする。予定期間内であることだけを根拠に`CURRENT`へ昇格しない。
- 終了日・撤収・支援先切替を公式に確認した場合は`HISTORICAL`へ移す。
- 新しい一次情報を取得できない場合は`UNKNOWN`とし、古い人数・団体名を現況値として残さない。「最新状況要確認」と注記するだけで旧値を現況表示に残すことは禁止する。
- 404、検索結果なし、新しい発表なしを「終了」と解釈しない。
- 熊本県等の中央集計と派遣元自治体の個別公表は対象時点・定義が異なり得る。無理に一致させず、それぞれの時点を明示する。
- 二次情報は新しい一次情報を発見するためにのみ利用し、CURRENT/HISTORICALの最終裁定根拠にはしない。
- `SUPPORT_BLOCKS`等の地域ブロックは、旧割当履歴であることを明示し、現在活動中の団体数として表示しない。

実装上は次を必須とする。

```sh
npm run sync:municipal-support
npm run validate:municipal-support
```

`validate:municipal-support`は、監査対象レコードの欠落、旧名簿構成団体の未裁定、source ID不整合、source/public不一致、`PAGE_RECHECK_META.checkedAt`との時点不一致、release_id不一致、生成overlayの欠落を検査する。

### 8.2 国・関係機関支援の全件再監査（必須）

全ての`timepoint_refresh`で、`provider: national`および画面上「国・関係機関」に分類する全国組織を独立した更新対象として全件再監査する。対象は固定リストだけでなく、現行ダッシュボードのコードから機械的に抽出し、内閣府、消防庁、国土交通省、厚生労働省、防衛省、警察庁、その他現行表示に存在する国機関、日本赤十字社・JMAT・JRAT等の全国組織を漏れなく確認する。

監査結果は`national-support-audit.json`へ保存し、`reference_at`と`release_id`をページおよびUPDATE_LEDGERと一致させる。`scripts/sync-national-support-audit.mjs`を唯一の生成経路とし、`scripts/validate-national-support-audit.mjs`を通常buildとRelease Gateの双方から必ず実行する。

必須ルール：

- 古い資料の値を「最新要確認」「継続要確認」だけでCURRENT表示しない。現在状態を確定できなければUNKNOWNとし、旧値はHISTORICALとしてのみ保持する。
- 派遣予定・出発予定・予定期間内であることだけを根拠にCURRENTへ昇格しない。実働確認前はPLANNEDとする。
- 最新発表がないこと、404、検索結果がないことを終了根拠にしない。終了・撤収・活動完了を公式に確認できた場合だけHISTORICALへ移す。
- 過去累計値と現在活動人数を同一指標として扱わない。TEC-FORCE等は「現時点人数」と「累計人日」を明確に分離する。
- 省庁ごとに`source_as_of`が異なる値を、同一時点の全国集計のように表示しない。監査時点`reference_at`と資料対象時点`source_as_of`を別表示する。
- CURRENTの最終根拠は原則として一次情報とし、二次情報は一次資料発見・クロスチェック用途に限定する。
- 愛媛県由来は愛媛県支援、自治体由来は`municipal-support-audit.json`へ分離し、national側へ二重計上しない。

全てのtimepoint refreshは、`municipal-support-audit.json`と`national-support-audit.json`の双方が最新`reference_at`と一致し、各validatorがPASSしなければ完了扱いにしない。

実装上は次を必須とする。

```sh
npm run sync:municipal-support
npm run sync:national-support
npm run validate:municipal-support
npm run validate:national-support
```

Release Gateは`national_support_checked: true`を出力し、監査ファイル欠落、reference_at不一致、既存nationalレコードの未裁定、CURRENTの一次情報不足、UNKNOWN旧スナップショットのcurrent表示、HISTORICAL/PLANNEDの現況誤表示をFAILさせる。

## 9. 現在避難所 candidateフロー

本番ファイルへ直接書く前にcandidateを生成する。

```sh
npm run update:current-shelters -- \
  --dry-run \
  --candidate-output /tmp/current-shelters-candidate.json

node scripts/validate-current-shelters.mjs \
  --candidate=/tmp/current-shelters-candidate.json
```

candidateではschema、`meta.current_count`と配列件数、facility ID、municipality、緯度経度、source metadata、unresolved、coordinate conflict、206点座標マスター整合を確認する。

PASS後のみ本番`current-shelters.json`へ反映する。公式JSONの現行定義は`startあり / endなし`。206点履歴を現行件数へ足し戻さない。100m超座標差は自動採用しない。

## 10. source / public / dist

正本は原則source HTML。

```sh
cp ehime_kumamoto_support_geocoded_shelters_20260802.html public/dashboard.html
npm run generate:current-shelter-data
npm run sync:page-metadata
cmp --silent ehime_kumamoto_support_geocoded_shelters_20260802.html public/dashboard.html
```

`sync:page-metadata`は`PAGE_RECHECK_META.checkedAt`（及び`volunteerCheckedAt`）を正として、ヘッダー、確認バッジ、概要、ページ再確認欄、タイムライン、支援ダッシュボード、現況snapshotの表示時刻を自動同期する。これらを個別手修正してはならない。

`npm run build`後はRelease Gateがsource/public、source/dist dashboard、CSS/JS/JSONのdist、Sites Workerへの埋め込み、index.htmlの正本参照に加え、現況表示時刻が`PAGE_RECHECK_META`と一致していることを自動検査する。

## 11. release_id / cache bust

`volunteer-data.js?v=<release_id>`をsource/public/dist/Workerで一致させ、台帳がある場合は台帳`release_id`とも一致させる。validator内の旧release_id残存も検査する。

## 12. validator分類

時点依存：`validate:dashboard`、`validate:current-state`、`validate:volunteer`、`validate:municipal-support`、`validate:dist`

構造・品質：`validate:shelters`、`validate:current-shelters`

Release Gate：`validate:release`

Release Gate自己テスト：`test:release-gate`

「3本のvalidator」等の固定本数表現は使用しない。

## 13. Release Gate自己テスト

`npm run test:release-gate`は一時Gitリポジトリへfixtureを複製し、実validatorへ故障を注入する。

常時検証：

1. source/public不一致 → FAIL
2. release_id不一致 → FAIL
3. expected_changed_files不一致 → FAIL
4. candidate count不一致 → FAIL
5. coordinate conflict → FAIL
6. 画面上の現況確認時刻と`PAGE_RECHECK_META`の不一致 → FAIL
7. current metadata旧時点 → FAIL
8. HISTORYの過去日付 → 誤検出せずPASS
9. accepted_unresolvedの適正記録 → PASS
10. blocking_unresolvedあり → FAIL
11. 対口支援・他自治体支援の監査時点が`PAGE_RECHECK_META`より古い／監査欠落 → FAIL

validator自体を変更してFAIL条件が消えた場合、CIで検出する。

## 14. GitHub Actions

main向けPRで`.github/workflows/validate-update.yml`を実行する。通常CIはlive公式JSONを取得せず、checked-inデータとfixtureを検証する。

CI実行内容：Nodeセットアップ、`npm run build`、build再現性確認、JS syntax check、`npm run test:release-gate`、`git diff --check`、`npm run validate:release -- --base=<PR base> --require-ledger-if-data-change`。

データ変更があるのに台帳がない場合はFAIL。

## 15. Branch Protection / Ruleset

Release Gateを「推奨CI」ではなくmerge条件にするため、GitHubリポジトリ設定側でmainを保護する。

推奨必須設定：

- mainへのPR経由を必須
- `Build and release gate` の成功を必須status check化
- merge前にbranchを最新mainへ追随させる
- mainへの通常直接pushを禁止
- 管理者のbypassは緊急時のみ

これはリポジトリ内コードではなくGitHubサーバ側設定である。設定変更権限/APIが利用できない実行環境では、未設定を「完了」と報告してはならない。

## 16. PR前ローカルゲート

```sh
npm run build
npm run test:release-gate
npm run validate:municipal-support
node --check volunteer.js
node --check volunteer-data.js
node --check scripts/build-sites.mjs
node --check scripts/validate-update-release.mjs
node --check scripts/test-release-gate.mjs
git diff --check
npm run validate:release -- --staged --ledger=<ledger-path>
```

## 17. PR / merge

PR本文に開始時main SHA、reference_at/release_id、主要sourceと対象時点、定義差、expected_changed_files、blocking/accepted unresolved、candidate validation、build/self-test/Release Gate、source/public/dist一致、残存リスクを記録する。

CI成功後にhead SHAを固定してmergeする。

## 18. 公開後QA

merge後main SHAを記録し、Pages実配信後にroot、本体HTML、CSS/JS/JSON、Desktop/Mobile、6タブ、出典リンク、地図、Console、current/history/plan表示、release_id/cache、実配信内容を確認する。GitHub PagesとSitesを混同しない。

main更新時は`.github/workflows/post-deploy-pages-smoke.yml`が、Pages上のHTML/CSS/JS/JSONとmerged mainのバイト一致、現況確認時刻、release_id、Desktop/Mobile runtime、6ビュー、page/console errorを自動検査する。workflow PASSを公開後QAの機械的証跡とし、最終目視はこれを補完する。

## 19. 残存する人間判断

完全自動化しない事項：公式情報の意味解釈、PLANNED→ACTUALの裁定、404と募集終了の区別、異なる集計定義の裁定、accepted_unresolvedが本当に安全かの判断、Desktop/Mobile最終目視。

人間判断が必要であること自体はエラーではない。推定で埋めず、状態・根拠・次回レビューを記録する。

## 20. 最終ゲート

- [ ] 最新mainから開始
- [ ] schema v2台帳をGit管理
- [ ] blocking_unresolved = 0
- [ ] accepted_unresolvedは必須メタデータ完備
- [ ] 45市町の確認状態に未処理なし
- [ ] 対口支援・他自治体支援を旧名簿の全構成団体まで再監査
- [ ] `municipal-support-audit.json`の`reference_at` = `PAGE_RECHECK_META.checkedAt`
- [ ] CURRENT / HISTORICAL / PLANNED / UNKNOWNを派遣元ごとに裁定
- [ ] `npm run validate:municipal-support` PASS
- [ ] candidate validator PASS
- [ ] source/public一致
- [ ] release_id一致
- [ ] 現況表示時刻が`PAGE_RECHECK_META`と一致
- [ ] `npm run build` PASS
- [ ] `npm run test:release-gate` PASS
- [ ] `npm run validate:release` PASS
- [ ] PR CI PASS
- [ ] main保護設定を確認
- [ ] merge後main SHA記録
- [ ] Pages実配信確認
- [ ] Desktop/Mobile・6タブ確認

前回と同種の問題を公開後の追加PRで修正するのではなく、可能な限りPR merge前に機械的に停止することを本運用の原則とする。
