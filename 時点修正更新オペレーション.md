# 時点修正更新オペレーション

> Version 2.5（2026-08-25改訂）
>
> Version 2.4までのRelease Gate、対口支援・他自治体支援監査、国・関係機関支援監査、Institution Coverage Gateに加え、**Source Mention Closure（一次資料中の支援主体言及の閉包）**、**record状態と部分指標状態の分離**、**same-reference re-adjudication**、**final-head CI / Pages smoke同期**を必須化する。
>
> 固定リスト、現行画面、前回監査のいずれか一つを確認しただけでは完了としない。固定最低確認先、現行ダッシュボード、前回監査、最新の政府・熊本県等の横断資料、さらに各一次資料本文・別紙・派遣元公式ページ・全国組織公式ページに登場する支援主体まで機関集合へ取り込み、**required_entities − adjudicated_entities = 0**かつ**source_mentions − adjudicated_entities = 0**を確認してから時点修正を完了する。

## 1. 目的

熊本県への支援・受援ダッシュボードを、公式情報の対象時点と定義を壊さず、再現可能な手順で更新し、GitHub Pages実配信まで一貫して検証する。

対象リポジトリ：`ryotamatsuki/kumamotoshienmap`

完了条件は次の全て。

1. 最新mainから開始する。
2. `reference_at`、`source_as_of`、公表日時、取得日時、確認日時を分離する。
3. CURRENT / HISTORICAL / PLANNED / UNKNOWN / CONFLICT / NOT_APPLICABLEを区別する。
4. 熊本県内45市町村、対口支援・他自治体支援、国・関係機関、全国組織を漏れなく再監査する。
5. Institution Coverage GateとSource Mention Closureを両方通す。
6. source / public / dist / Pages実配信の整合を確認する。
7. build、validator、Release Gate、自己テスト、通常CIを通す。
8. merge後、GitHub Pagesの実配信をDesktop/Mobile双方で確認する。

## 2. 時点の定義

| フィールド | 意味 |
|---|---|
| `reference_at` | 今回の判断対象となる基準日時（JST） |
| `source_as_of` | 公式資料が表す対象時点 |
| `source_published_at` | 資料の公表日時 |
| `source_updated_at` | 公式ページ・JSON等の更新日時 |
| `fetched_at` | API・JSON等の取得日時 |
| `checked_at` | 内容を確認した日時 |
| `rechecked_at` | 同一`reference_at`について追加再確認した日時 |
| `page_checked_at` | ページ全体を最後に実機確認した日時 |
| `release_id` | `YYYYMMDD-HHMM`形式の更新識別子 |
| `deployed_sha` | 公開反映後のmain SHA |

これらを相互に代用しない。

### 2.1 same-reference再裁定

同じ`reference_at`について後から追加一次情報を見つけてUNKNOWN等を再裁定する場合、`reference_at`は動かさず、`rechecked_at`を別に記録する。

禁止事項：

- 追加再監査の時刻で`reference_at`を書き換える。
- 「全件再監査済み」の証跡を「追加再監査」の文言だけで上書きする。
- 追加情報で旧snapshot・旧`source_as_of`を消す。

表示上は必要に応じて「全件再監査基準＋追加再監査」と併記する。

## 3. 状態管理

| 状態 | 意味 |
|---|---|
| CURRENT | 基準日時点で適用中・活動中と一次情報等で確認済み |
| HISTORICAL | 終了済み・過去実績 |
| PLANNED | 公式予定だが実施・実働未確認 |
| UNKNOWN | 公式情報不足・取得失敗・同一定義不明等で確認不能 |
| CONFLICT | 一次情報間で対象時点・定義・値が競合 |
| NOT_APPLICABLE | 公式に対象外、または今回の表示対象外と合理的に確認済み |

### 3.1 record状態と部分指標状態を分離する

record全体の活動状態と、人数・台数・全国総数・県全体集計・現金交付完了等の部分指標は別軸で裁定する。

例：

- 派遣元公式ページで緊急消防援助隊の現行活動を確認できればrecordはCURRENTにできる。一方、消防・警察・自衛隊・航空を束ねた旧定義の全国複合総数が確定できない場合、その総数だけUNKNOWNとして残す。
- 保健医療福祉支援の現行派遣例を一次確認できても、DHEAT / DMAT / DPAT / 日赤等を束ねた同一定義全国総数が不明なら集計値はUNKNOWN。
- 普通交付税の繰上げ交付「決定」を確認できても、現金交付「完了」を直接確認できなければ、決定済みと実施完了UNKNOWNを分離する。
- 8月18日のプッシュ型物資支援強化を確認できても、`reference_at`時点で同一スキーム継続を直接確認できなければrecordはUNKNOWNのままとする。

`accepted_unresolved`はentity未監査の逃げ道ではない。entity自体は必ず裁定し、未確定なのが活動状態か、数量・集計・実施完了等の部分指標かを明示する。

### 3.2 禁止事項

- 404を「終了」「募集なし」に変換しない。
- 検索結果なし、新着なしを「終了」とみなさない。
- 予定人数を実績人数に変換しない。
- 個人募集から団体受入れを推定しない。
- 古い人数・台数・団体数に「要確認」とだけ付けてCURRENT表示を継続しない。
- 新しい公表がないことだけを根拠にHISTORICALへ移さない。
- 未公表値を0や推計値へ変換しない。
- 過去累計と現在活動人数を同一指標として扱わない。

## 4. 主要ファイル

| 種類 | ファイル |
|---|---|
| Pages入口 | `index.html` |
| Pages本体・正本 | `ehime_kumamoto_support_geocoded_shelters_20260802.html` |
| Sites公開コピー | `public/dashboard.html` |
| 現在避難所 | `current-shelters.json` |
| 座標履歴 | HTML内座標マスター / `shelter-coordinate-manifest.json` |
| ボランティア調査 | `research_official_*.json` |
| ボランティア生成物 | `volunteer-data.js` |
| 対口・自治体監査 | `municipal-support-audit.json` |
| 国・関係機関監査 | `national-support-audit.json` |
| Release Gate | `scripts/validate-update-release.mjs` |
| Gate自己テスト | `scripts/test-release-gate.mjs` |
| 台帳schema | `scripts/update-ledger.schema.json` |
| 通常CI | `.github/workflows/validate-update.yml` |
| 公開後QA | `.github/workflows/post-deploy-pages-smoke.yml` |

過去更新用migration、fixer、一時workflow、一時markerは最終PR差分へ残さない。

## 5. 更新開始

```sh
git fetch origin
git status --short
git log -1 --oneline origin/main
git switch -c audit/timepoint-YYYYMMDD-HHMM origin/main
```

古い参考SHAへresetしない。作業開始時main SHAをUPDATE_LEDGERとPR本文へ記録する。

## 6. UPDATE_LEDGER schema v2

実更新PRでは台帳をGit管理する。

推奨：`operations/ledgers/refresh-YYYYMMDD-HHMM.json`

最低限：

```json
{
  "schema_version": 2,
  "repository": "ryotamatsuki/kumamotoshienmap",
  "update_type": "timepoint_refresh",
  "gate_phase": "pre_merge",
  "update_id": "refresh-YYYYMMDD-HHMM",
  "release_id": "YYYYMMDD-HHMM",
  "reference_at": "YYYY-MM-DDTHH:MM:SS+09:00",
  "page_checked_at": null,
  "base_main_sha": "40文字SHA",
  "expected_changed_files": [],
  "sources": [],
  "coverage": {},
  "snapshots": {},
  "blocking_unresolved": [],
  "accepted_unresolved": []
}
```

### 6.1 blocking / accepted unresolved

`blocking_unresolved`が1件でもあればmerge不可。

`accepted_unresolved`には最低限、`issue_id`、`state`、`reason`、`owner`、`source_ids`、`next_review_at`を持たせる。UNKNOWN等を安全に明示して公開するために使い、不都合な事実を隠すために使わない。

### 6.2 coverageに残す証跡

```json
{
  "institution_audit": {
    "fixed_minimum_checked": true,
    "dashboard_inventory_count": 0,
    "previous_audit_inventory_count": 0,
    "official_discovery_count": 0,
    "source_mention_count": 0,
    "source_mention_adjudicated_count": 0,
    "unadjudicated_source_mentions": [],
    "required_entity_count": 0,
    "adjudicated_entity_count": 0,
    "missing_entities": [],
    "newly_discovered_entities": []
  }
}
```

`missing_entities`または`unadjudicated_source_mentions`が1件でもあればmerge不可。

## 7. Institution Coverage Gate

### 7.1 required_entities

`required_entities`は次の和集合とする。

1. 固定最低確認先。
2. 現行ダッシュボードHTML/JS/JSONに登場するprovider、record、source、支援主体、出典機関。
3. `municipal-support-audit.json`、`national-support-audit.json`、直前UPDATE_LEDGERの全entity。
4. 最新の政府・熊本県・各省庁等の横断公式資料で新たに発見した機関。
5. 表示外でも現況に関係する新規機関。
6. 採用した一次資料本文・表・別紙・派遣元自治体公式ページ・全国組織公式ページに活動主体・派遣主体・共同主体として登場する全機関（`source_mentions`）。

完了条件：

```text
required_entities - adjudicated_entities = ∅
source_mentions - adjudicated_entities = ∅
```

さらに以下を満たすこと。

```text
current_dashboard_entities ⊆ required_entities
previous_audit_entities ⊆ required_entities
officially_discovered_entities ⊆ required_entities
source_mentions ⊆ required_entities
```

### 7.2 固定最低確認先

少なくとも以下を毎回の探索起点とする。

- 熊本県、熊本県内45市町村、熊本県社協、市町村社協。
- 愛媛県および現行表示・派遣実績に登場する愛媛県内主体。
- 対口支援・応急対策職員派遣制度等の全派遣元・構成団体。
- 内閣府、防災関係政府組織、総務省、消防庁、国土交通省、関係地方整備局、TEC-FORCE、厚生労働省、防衛省・自衛隊、警察庁。
- 農林水産省、経済産業省、環境省、文部科学省、法務省、財務・国税関係等、最新横断資料に登場し得る省庁・地方支分部局。
- DHEAT、DMAT、DPAT、DWAT、災害支援ナース、日本赤十字社、日本医師会/JMAT、JRAT、日本薬剤師会等、現行表示・最新公式資料に登場する全国組織。

固定最低確認先は「常にCURRENT」という意味ではない。新規参画を見落とさないための探索対象である。

## 8. Source Mention Closure

機関漏れ防止のため探索は最低3パスで行う。

### 8.1 Inventory Pass

固定最低確認先、現行ダッシュボード、前回municipal/national監査、直前UPDATE_LEDGERから既知entityを列挙する。

### 8.2 Cross-cutting Discovery Pass

内閣府、熊本県、各省庁災害ページ、政府横断資料、対口支援名簿等から新規entityを探索する。

### 8.3 Source Mention Closure Pass

1・2で採用した各一次資料を本文まで読み、そこに登場する派遣元自治体、地方支分部局、全国組織、職能団体、共同実施主体を逆引きする。未裁定主体がなくなるまでrequired_entitiesへ追加して再探索する。

各`source_id`について最低限確認する。

- 公表主体は誰か。
- 本文・表・別紙に別の活動主体・派遣主体・共同主体が出ていないか。
- その主体がrequired_entitiesへ入っているか。
- その主体に状態裁定があるか。
- 表示しない場合でも監査上の除外理由が残っているか。

中央省庁ページに全国現況総数がなくても、派遣元自治体・地方支分部局・全国組織の公式ページで活動継続を確認できることがある。その場合、活動状態CURRENTの根拠には使えるが、旧来の全国複合総数をCURRENTへ推定更新してはならない。

## 9. 分野別再監査

### 9.1 対口支援・他自治体支援

- 旧割当名簿を現在活動中とみなさない。
- 旧名簿の全構成団体を1団体ずつ再探索する。
- 期間内というだけでCURRENTへ昇格しない。
- 終了・撤収・支援先切替を公式確認した場合のみHISTORICALへ移す。
- 新しい一次情報を取得できなければUNKNOWNとし、古い値を現況表示に残さない。
- 中央集計と派遣元個別公表の対象時点・定義が違う場合は無理に一致させない。

```sh
npm run sync:municipal-support
npm run validate:municipal-support
```

### 9.2 国・関係機関・全国組織

対象はrequired_entitiesからnational/全国組織を抽出した集合とする。12件、13件等の固定件数を次回監査の母数にしてはならない。

- 古い資料の値を「継続要確認」だけでCURRENT表示しない。
- 派遣予定・出発予定だけならPLANNED。
- 最新発表がないこと、404、検索結果なしを終了根拠にしない。
- TEC-FORCE等は現時点人数と累計人日を分離する。
- 省庁ごとに`source_as_of`が違う値を同一時点全国集計のように表示しない。
- CURRENTの最終根拠は原則一次情報。
- 愛媛県由来は愛媛県支援、自治体由来はmunicipalへ分離し、nationalへ二重計上しない。
- 値が取得しにくい機関も監査対象から外さずUNKNOWNとして残す。

```sh
npm run sync:national-support
npm run validate:national-support
```

## 10. 現在避難所candidateフロー

本番へ直接書く前にcandidateを生成する。

```sh
npm run update:current-shelters -- \
  --dry-run \
  --candidate-output /tmp/current-shelters-candidate.json

node scripts/validate-current-shelters.mjs \
  --candidate=/tmp/current-shelters-candidate.json
```

schema、件数、facility ID、municipality、緯度経度、source metadata、unresolved、coordinate conflict、座標マスター整合を確認する。100m超座標差は自動採用しない。履歴座標を現行件数へ足し戻さない。

## 11. same-reference re-adjudication

同一`reference_at`で追加一次情報によりUNKNOWN等を再裁定する場合：

1. `reference_at`は維持し`rechecked_at`を追加する。
2. 元の全件再監査証跡を残す。
3. 追加一次情報からrequired_entities / source_mentionsが増えた場合、Institution Coverage GateとSource Mention Closureを再実施する。
4. 旧snapshot、旧`source_as_of`をHISTORICAL等として保持する。
5. sync script、validator、Release Gate自己テスト、表示HTML、post-deploy smokeの固定文言・期待値を同じPRで同期する。
6. 一時migrationやbot生成commit後は、最終head SHAで通常CIを取り直す。
7. 一時workflow / migration / markerを最終差分から除去し、`expected_changed_files`を実差分と一致させる。

## 12. source / public / dist / release_id

正本は原則source HTML。

```sh
cp ehime_kumamoto_support_geocoded_shelters_20260802.html public/dashboard.html
npm run generate:current-shelter-data
npm run sync:municipal-support
npm run sync:national-support
npm run sync:page-metadata
cmp --silent ehime_kumamoto_support_geocoded_shelters_20260802.html public/dashboard.html
```

`PAGE_RECHECK_META.checkedAt`をページ全体確認時刻の正とし、個別にhard-codeしない。

`volunteer-data.js?v=<release_id>`、source/public/dist/Worker、UPDATE_LEDGERの`release_id`を一致させる。

`expected_changed_files`と実PR差分は完全一致させる。

## 13. validator / Release Gate

時点依存validator：`validate:dashboard`、`validate:current-state`、`validate:volunteer`、`validate:municipal-support`、`validate:national-support`、`validate:dist`

構造・品質：`validate:shelters`、`validate:current-shelters`

Release Gate：`validate:release`

自己テスト：`test:release-gate`

最低限の故障注入：

1. source/public不一致 → FAIL
2. release_id不一致 → FAIL
3. expected_changed_files不一致 → FAIL
4. candidate count不一致 → FAIL
5. coordinate conflict → FAIL
6. `PAGE_RECHECK_META`不一致 → FAIL
7. current metadata旧時点 → FAIL
8. HISTORICAL過去日付 → 誤検出せずPASS
9. accepted_unresolved適正記録 → PASS
10. blocking_unresolvedあり → FAIL
11. municipal監査欠落・旧時点 → FAIL
12. national監査欠落・旧時点 → FAIL
13. national既存record未裁定 → FAIL
14. UNKNOWN旧snapshotをcurrent表示 → FAIL
15. `missing_entities > 0` → FAIL
16. `unadjudicated_source_mentions > 0` → FAIL
17. same-reference再裁定で元の全件再監査証跡が消失 → FAIL
18. record CURRENTと部分指標UNKNOWNの区別が失われ、旧全国総数等をCURRENT表示 → FAIL

未実装のGateでも、15〜18は文書運用上のmerge blockerとする。

## 14. GitHub Actions / final-head CI

main向けPRでは`.github/workflows/validate-update.yml`を実行する。

CIは最低限、Nodeセットアップ、`npm run build`、build非書換確認、JS syntax check、`npm run test:release-gate`、`git diff --check`、`npm run validate:release`を含む。

**CIのPASSはcommit SHA単位。** bot commit、一時migrationによる生成commit、cleanup commit、台帳修正commit等でPR headが動いた場合、それ以前のPASSは最終判定に使わない。

最終CI前に：

- 一時workflow / migration / fixer / markerを除去。
- `expected_changed_files`を最終実差分へ確定。
- final head SHAを取得。
- final headに対して通常CIを再実行。

merge対象head SHAとCI PASS対象SHAは同一でなければならない。

## 15. PR / merge

PR本文へ最低限記録する。

- 開始時main SHA
- `reference_at` / `rechecked_at` / `release_id`
- 主要sourceと`source_as_of`
- required/adjudicated/missing/newly discovered entity数
- source_mentions / unadjudicated_source_mentions
- municipal/national監査結果
- record状態と部分指標UNKNOWNの分離状況
- expected_changed_files
- blocking/accepted unresolved
- candidate validation
- build/self-test/Release Gate
- source/public/dist一致
- 残存リスク

CI成功後、expected head SHAを固定してmergeする。

## 16. 公開後QA

merge後main SHAを記録し、GitHub Pages実配信を確認する。

順序：

1. 公開HTMLとmerged mainのHTMLがバイト一致することを確認。
2. 主要asset（CSS/JS/JSON）もmerged mainと一致することを確認。
3. rootから本体HTMLへの導線を確認。
4. Desktop 1440px級、Mobile 390px級で実ブラウザ検査。
5. `overview / needs / timeline / dashboard / volunteer / map`の6ビュー確認。
6. 重要national/municipal表示、CURRENT/HISTORICAL/PLANNED/UNKNOWN、出典リンク、地図を確認。
7. page error / console errorが0件であることを確認。

表示文言・監査ラベル・重要national状態を変更したPRでは、**post-deploy smokeの固定期待値もmerge前に同一PRで同期**する。公開後に旧文言を期待するsmokeが誤FAILする状態を残さない。

非表示詳細をブラウザQAで検査するとき、`body.innerText`だけに依存しない。対象DOMの`textContent`、明示selector、または`document.documentElement.innerHTML`等、実際の表示・DOM構造に合う方法を使う。

## 17. Branch Protection推奨

- mainへのPR経由を必須化。
- `Build and release gate`成功を必須status check化。
- merge前にbranchを最新mainへ追随。
- 通常のmain直接pushを禁止。
- 管理者bypassは緊急時のみ。

設定できない場合は、未設定を完了扱いにしない。

## 18. 最終チェックリスト

### 機関カバレッジ

- [ ] 固定最低確認先を探索
- [ ] 現行ダッシュボードから全provider / record / source / 機関を抽出
- [ ] 前回municipal/national監査の全entityを継承
- [ ] 最新政府・熊本県等の横断資料から新規機関を探索
- [ ] 採用した全一次資料本文・表・別紙から`source_mentions`を抽出
- [ ] 各source_idから公表主体・派遣主体・共同主体を逆引き
- [ ] 新規機関を`newly_discovered_entities`へ記録
- [ ] required_entitiesを確定
- [ ] 全required entityを状態裁定
- [ ] `missing_entities = []`
- [ ] `unadjudicated_source_mentions = []`
- [ ] required_entities件数 = adjudicated_entities件数
- [ ] record状態と部分指標・全国集計UNKNOWNを分離

### データ・監査

- [ ] 最新mainから開始
- [ ] schema v2台帳をGit管理
- [ ] blocking_unresolved = 0
- [ ] accepted_unresolvedの必須メタデータ完備
- [ ] 45市町の確認状態に未処理なし
- [ ] 対口支援・他自治体支援を旧名簿の全構成団体まで再監査
- [ ] 国・関係機関・全国組織を全件再監査
- [ ] same-reference追加再裁定ならreference_at維持・rechecked_at記録・元の全件再監査証跡維持
- [ ] municipal/national監査のreference_at / release_id整合
- [ ] candidate validator PASS

### Build / Release

- [ ] source/public一致
- [ ] release_id一致
- [ ] `npm run build` PASS
- [ ] build後にtracked inputを書き換えていない
- [ ] `npm run test:release-gate` PASS
- [ ] `npm run validate:release` PASS
- [ ] expected_changed_filesと実PR差分が完全一致
- [ ] 一時workflow / migration / fixer / markerが最終差分に残っていない
- [ ] merge対象final head SHAとCI PASS対象SHAが同一
- [ ] bot生成・cleanup・台帳調整後にfinal-head CIを再実行済み
- [ ] merge時head SHA固定
- [ ] merge後main SHA記録

### Pages実配信

- [ ] Pages実配信HTML・主要assetがmerged mainとバイト一致
- [ ] 表示文言変更時にpost-deploy smoke期待値を同一PRで同期
- [ ] post-deploy Pages smoke PASS
- [ ] Desktop/Mobile・6ビュー確認
- [ ] Console / page errorなし
- [ ] national/municipal主要表示と公式リンク確認

前回と同種の問題を公開後の追加PRで修正するのではなく、可能な限りmerge前に機械的Gateまたはチェックリストで停止することを本運用の原則とする。
