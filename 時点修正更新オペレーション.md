# 時点修正更新オペレーション

> Version 2.6（2026-08-27改訂）
>
> Version 2.5までのRelease Gate、対口支援・他自治体支援監査、国・関係機関支援監査、Institution Coverage Gate、Source Mention Closure、record状態と部分指標状態の分離、same-reference re-adjudication、final-head CI / Pages smoke同期を**すべて維持**する。
>
> Version 2.6ではこれに加え、**愛媛県内20市町のsender-side Exhaustive Hard Gate**、**全国47都道府県sender candidate discovery Gate**、**発見済み基礎自治体senderの完全裁定**、**sender source-mention closure**、**公開sender UIとPages Production QA**を必須化する。
>
> recipient-sideの熊本県45市町村Coverageと、sender-sideの全国基礎自治体Coverageを混同しない。固定リスト、現行画面、前回監査、既知の指定都市だけを確認して「全国監査済み」としてはならない。

## 1. 目的

熊本県への支援・受援ダッシュボードを、公式情報の対象時点と定義を壊さず、再現可能な手順で更新し、GitHub Pages実配信まで一貫して検証する。

対象リポジトリ：`ryotamatsuki/kumamotoshienmap`

完了条件は次の全て。

1. 最新`origin/main`から開始し、古い参考SHAへresetしない。
2. `reference_at`、`source_as_of`、公表日時、取得日時、確認日時を分離する。
3. 既存Version 2.5の全監査・全validatorを通す。
4. 熊本県内45市町村、対口支援、国・関係機関、全国組織を再監査する。
5. Institution Coverage GateとSource Mention Closureを通す。
6. 愛媛20市町をsenderとして20/20個別裁定する。
7. 全国47都道府県についてsender discoveryを47/47完了する。
8. discoveryで発見した全基礎自治体senderを裁定する。
9. sender source mention orphanを0にする。
10. source / public / dist / Pages実配信の整合を確認する。
11. build、validator、Release Gate、自己破壊テスト、通常PR CIを通す。
12. squash merge後、Pages実配信をDesktop/Mobile双方で確認する。

## 2. 時点の定義

| フィールド | 意味 |
|---|---|
| `reference_at` | その監査の判断対象となる基準日時（JST） |
| `source_as_of` | 公式資料が表す対象時点 |
| `source_published_at` | 資料の公表日時 |
| `source_updated_at` | 公式ページ・JSON等の更新日時 |
| `fetched_at` | API・JSON等の取得日時 |
| `checked_at` | 内容を確認した日時 |
| `rechecked_at` | 同一`reference_at`について追加再確認した日時 |
| `page_checked_at` | ページ全体を最後に実機確認した日時 |
| `release_id` | `YYYYMMDD-HHMM`形式の更新識別子 |
| `deployed_sha` | 公開反映後のmain SHA |

これらを相互に代用しない。

### 2.1 ページ全体時点とsender監査時点

ページ全体の被害・避難所・ボランティア・municipal/national監査を再実施していないのに、sender監査だけ新しく行った時刻へページ全体`reference_at`を動かしてはならない。

sender監査は`sender-municipality-audit.json.reference_at`を独立して持つことができる。画面上も「ページ全体確認時点」と「sender監査基準時点」を混同しない。

### 2.2 same-reference再裁定

同じ`reference_at`について後から追加一次情報を見つけてUNKNOWN等を再裁定する場合、`reference_at`は動かさず`rechecked_at`を別に記録する。

禁止事項：

- 追加再監査の時刻で`reference_at`を書き換える。
- 元の全件再監査証跡を追加再監査文言で上書きする。
- 追加情報で旧snapshot・旧`source_as_of`を消す。

## 3. 状態管理

### 3.1 既存データ領域

既存の被害・避難所・municipal/national等では以下を維持する。

- `CURRENT`
- `HISTORICAL`
- `PLANNED`
- `UNKNOWN`
- `CONFLICT`
- `NOT_APPLICABLE`

### 3.2 sender municipality領域

sender-side基礎自治体では以下を使用する。

- `CURRENT`
- `PLANNED`
- `HISTORICAL`
- `UNKNOWN`
- `NO_EVIDENCE`
- `CONFLICT`

`NO_EVIDENCE`は「定義した一次情報探索を実施したが、当該災害への送出支援記述自体を確認できなかった」の意味であり、「支援していない」という断定ではない。

`UNKNOWN`は支援・派遣・協力の言及はあるが、基準時点の実働・終了・継続状態を確定できない場合に使用する。

### 3.3 CURRENTの厳格条件

予定期間に入っただけでCURRENTへ昇格しない。

「派遣予定」「派遣する」「○日～○日予定」「出発予定」しか根拠がなければ、基準時点が期間内でもCURRENTにしない。

CURRENTには原則として次の直接根拠が必要。

- 派遣中
- 活動中
- 現地で活動
- 支援しています
- 継続して派遣
- 現在○名
- 実派遣・実在地確認
- 現在受付中・現在実施中

sender auditの`current_evidence_type`は少なくとも次のいずれか。

- `explicit_current_activity`
- `continuation_explicit`
- `actual_dispatch_confirmed`
- `actual_presence_confirmed`
- `active_collection_explicit`

secondary/discovery sourceだけでCURRENTにしてはならない。

### 3.4 record状態と部分指標状態

record全体の活動状態と、人数・台数・全国総数・県全体集計等の部分指標は別軸で裁定する。

活動継続が一次確認できても同一定義の全国総数が分からなければ、recordはCURRENT、全国総数はUNKNOWNにできる。古い総数をCURRENTとして残してはならない。

## 4. 主要ファイル

| 種類 | ファイル |
|---|---|
| Pages入口 | `index.html` |
| Pages本体・正本 | `ehime_kumamoto_support_geocoded_shelters_20260802.html` |
| sender公開UI | `sender-municipalities.html` |
| Sites公開コピー | `public/dashboard.html` |
| 現在避難所 | `current-shelters.json` |
| 座標履歴 | HTML内座標マスター / `shelter-coordinate-manifest.json` |
| ボランティア調査 | `research_official_*.json` |
| ボランティア生成物 | `volunteer-data.js` |
| 対口・自治体監査 | `municipal-support-audit.json` |
| 国・関係機関監査 | `national-support-audit.json` |
| sender監査manifest | `sender-municipality-audit.json` |
| 愛媛sender台帳 | `sender-audit/ehime.json` |
| 全国sender地域台帳 | `sender-audit/*.json` |
| sender source catalog | `sender-audit/sources.json`, `sender-audit/sources-additions.json` |
| 47都道府県discovery | `sender-audit/discovery.json`, `sender-audit/discovery-additions.json` |
| sender詳細仕様 | `operations/SENDER_MUNICIPALITY_COVERAGE.md` |
| 既存Release Gate | `scripts/validate-update-release.mjs` |
| sender連結Release Gate | `scripts/validate-release-with-sender.mjs` |
| sender validator | `scripts/validate-sender-coverage.mjs` |
| sender Gate自己破壊テスト | `scripts/test-sender-coverage-gates.mjs` |
| 既存Gate自己テスト | `scripts/test-release-gate.mjs` |
| 通常CI | `.github/workflows/validate-update.yml` |
| 公開後QA | `.github/workflows/post-deploy-pages-smoke.yml` |

一時migration、one-shot fixer、debug marker、一時workflowは最終PR差分へ残さない。

## 5. 更新開始

```sh
git fetch origin
git status --short
git log -1 --oneline origin/main
git branch -r
gh pr list --state open
git switch -c audit/timepoint-YYYYMMDD-HHMM origin/main
```

開始時に必ず記録する。

- latest `origin/main` SHA
- open PR
- open branch / 未マージ作業
- 作業branch
- ページ全体`reference_at`
- sender監査`reference_at`

古い参考SHAへ無条件にcheckout/resetしない。

## 6. UPDATE_LEDGER schema v2

既存時点修正を伴うPRでは台帳をGit管理する。

推奨：`operations/ledgers/refresh-YYYYMMDD-HHMM.json`

`blocking_unresolved`が1件でもあればmerge不可。

`accepted_unresolved`には最低限、`issue_id`、`state`、`reason`、`owner`、`source_ids`、`next_review_at`を持たせる。

sender-only監査追加でページ全体時点を変更しない場合は、senderの独立manifestである`sender-municipality-audit.json`に`base_main_sha`、`reference_at`、coverage summary、blocking状態を保持する。

## 7. Institution Coverage / Source Mention Closure

既存Institution Coverage Gateは維持する。

`required_entities`は少なくとも次の和集合。

1. 固定最低確認先。
2. 現行ダッシュボードに登場するprovider、record、source、支援主体。
3. `municipal-support-audit.json`、`national-support-audit.json`、直前台帳の全entity。
4. 最新政府・熊本県・各省庁等の横断公式資料で新たに発見した機関。
5. 採用した一次資料本文・表・別紙・派遣元公式ページ・全国組織公式ページの全支援主体。

完了条件：

```text
required_entities - adjudicated_entities = ∅
source_mentions - adjudicated_entities = ∅
```

Source Mention Closureは最低3パスで行う。

1. Inventory Pass
2. Cross-cutting Discovery Pass
3. Source Mention Closure Pass

source snippetだけで本文を読んだことにしない。PDFはページ単位・表列単位で確認する。

## 8. Recipient-sideとSender-sideの分離

以下を別Coverageとして管理する。

- `recipient_municipality_coverage`：熊本県内45市町村等
- `sender_municipality_coverage`：愛媛20市町＋全国sender discoveryで発見された基礎自治体

旧Institution Coverageの`municipalities: 45`等をsender coverage件数へ流用してはならない。

## 9. 愛媛20市町 Sender Exhaustive Hard Gate

以下20市町を固定required universeとする。

1. 松山市
2. 今治市
3. 宇和島市
4. 八幡浜市
5. 新居浜市
6. 西条市
7. 大洲市
8. 伊予市
9. 四国中央市
10. 西予市
11. 東温市
12. 上島町
13. 久万高原町
14. 松前町
15. 砥部町
16. 内子町
17. 伊方町
18. 松野町
19. 鬼北町
20. 愛南町

毎回20市町すべてをゼロベースで確認する。既存データに名前がある自治体だけを確認してはならない。

最低探索範囲：

- 市町公式Web / press / 防災
- 市長・町長会見
- 職員派遣
- 消防
- 水道
- 住宅
- 物資
- 車両
- ボランティア
- 社協
- 医療
- 技術職
- 義援金・寄附代理受付

市役所、消防、水道、病院、社会福祉協議会等が別法人・別制度主体ならsub-actorとして分離する。

県資料の「市町40人」等を20市町へ推計按分しない。個別内訳がない場合はaggregateのまま保持し、個別assignmentはUNKNOWNとする。

Ehime Gate必須条件：

```text
required_count == 20
adjudicated_count == 20
missing_required == 0
unadjudicated_source_mentions == 0
blocking_unresolved == 0
```

UNKNOWN / NO_EVIDENCEは許容するが、必ず`checked_at`、`reason`、`sources_checked`、`next_review_at`を持つ。

## 10. 全国基礎自治体 Sender Discovery Gate

全国約1,700自治体の全ページcrawlをHard Gateにはしない。

代わりに二層で保証する。

### Layer A：Sender Candidate Discovery Coverage

47都道府県すべてに`discovery_state: CHECKED`を持たせる。

必須探索チャネル：

- 熊本県災害対策本部・受援自治体資料
- 総務省、内閣府、消防庁、国交省、厚労省、防衛省等
- 全国知事会、全国市長会、全国町村会、指定都市市長会
- 日本水道協会、全国社会福祉協議会等
- 47都道府県の熊本地震支援、職員派遣、対口支援、給水、保健師、消防、技術職等
- 上記で候補化した基礎自治体の公式情報
- 検索エンジン等による追加discovery
- 災害支援代理寄附等の横断レジストリ

候補0県でもよいが、未確認県は不可。

```text
prefecture_discovery_checked == 47
```

### Layer B：Discovered Sender Adjudication Closure

発見した全基礎自治体senderを裁定する。

対象：

- 市
- 町
- 村
- 東京都特別区

政令指定都市の行政区はmunicipality分母へ入れない。一部事務組合、消防組合、水道企業団等も別kindで保持し、municipality分母へ入れない。

全国entityの一意キーは必ず以下。

```text
prefecture + municipality_name
```

同名自治体が複数県に存在するため、市町村名単独をglobal keyにしてはならない。

完了条件：

```text
discovered_basic_municipality_senders - adjudicated_basic_municipality_senders = ∅
source_mentions_basic_municipalities - adjudicated_source_mentions_basic_municipalities = ∅
blocking_sender_conflicts == 0
```

## 11. Sender Source Mention Closure

一次資料又は採用した横断discovery資料に支援主体として出た市区町村がentity化されていない状態を禁止する。

誤検出防止のため、以下はsender mentionと区別する。

- 受援自治体名
- 単なる所在地
- 施設所在地
- 行政区名のみ

source mentionは都道府県を含む一意キーへ解決する。

## 12. Source Tier

優先順位：

- Tier 1：送出自治体公式一次情報
- Tier 2：送出都道府県公式一次情報
- Tier 3：熊本県・受援自治体公式一次情報
- Tier 4：国・全国団体公式情報
- Tier 5：報道・検索・横断discovery source

Tier 5は候補発見には使えるが、単独でCURRENTにしてはならない。

## 13. 分野別再監査

### 13.1 対口支援・他自治体支援

- 旧割当名簿をCURRENTとみなさない。
- 旧名簿の全構成団体を1団体ずつ再探索する。
- 期間内だけでCURRENTへ昇格しない。
- 終了・撤収・支援先切替を公式確認した場合のみHISTORICALへ移す。
- 新しい一次情報を取得できなければUNKNOWN。

```sh
npm run sync:municipal-support
npm run validate:municipal-support
```

### 13.2 国・関係機関・全国組織

- 固定件数を次回母数にしない。
- 派遣予定・出発予定だけならPLANNED又はUNKNOWN。
- 最新発表がないこと、404、検索結果なしを終了根拠にしない。
- 現時点人数と累計人日を分離する。
- source_as_ofが違う値を同一時点全国集計のように表示しない。

```sh
npm run sync:national-support
npm run validate:national-support
```

## 14. 現在避難所candidateフロー

本番へ直接書く前にcandidateを生成する。

```sh
npm run update:current-shelters -- \
  --dry-run \
  --candidate-output /tmp/current-shelters-candidate.json

node scripts/validate-current-shelters.mjs \
  --candidate=/tmp/current-shelters-candidate.json
```

schema、件数、facility ID、municipality、緯度経度、source metadata、unresolved、coordinate conflict、座標マスター整合を確認する。100m超座標差は自動採用しない。履歴座標を現行件数へ足し戻さない。

## 15. source / public / dist / UI

正本HTMLと`public/dashboard.html`を一致させる。

```sh
npm run generate:current-shelter-data
npm run sync:municipal-support
npm run sync:national-support
npm run sync:page-metadata
npm run validate:sender-coverage
npm run build
```

sender UIは`sender-municipalities.html`で公開し、最低限以下を表示する。

- 愛媛20市町別状態
- 全国sender件数
- 送出都道府県
- 送出市町村
- 状態
- 支援種別
- 派遣・支援先
- 根拠
- source link
- 都道府県・状態・検索filter

UNKNOWNは隠さない。NO_EVIDENCEを「支援なし」と書換えない。

## 16. Validator / Release Gate

必須コマンド：

```sh
npm run validate:dashboard
npm run validate:current-state
npm run validate:timeline
npm run validate:volunteer
npm run validate:shelters
npm run validate:current-shelters
npm run validate:municipal-support
npm run validate:national-support
npm run validate:institution-coverage
npm run validate:ehime-sender-coverage
npm run validate:nationwide-sender-coverage
npm run validate:sender-source-mentions
npm run validate:sender-coverage
npm run build
npm run test:release-gate
npm run validate:release -- --base=<BASE_SHA> [--ledger=<LEDGER>]
git diff --check
```

`validate:release`は既存Version 2.5 Release Gateを先に実行し、その後sender coverage gateを実行する。sender Gateを追加するために既存validatorを削除・弱体化してはならない。

## 17. Sender Gate自己破壊テスト

最低限、次を故障注入してFAILすることを確認する。

1. 松山市をrequired universeから削除
2. 今治市state=null
3. 宇和島市がsource mentionにあるのにentityなし
4. 所沢市source mentionのみでentityなし
5. 愛知県discovery未確認
6. schedule-only recordをCURRENT
7. UNKNOWNにreasonなし
8. 46/47都道府県しかCHECKEDでない
9. 東京都特別区senderを発見したが未裁定
10. 消防組合等をmunicipality分母へ誤算入

自己破壊テストを形骸化させるためvalidatorを弱めてはならない。

## 18. GitHub Actions / final-head CI

main向けPRでは`.github/workflows/validate-update.yml`を実行する。

CIは最低限：

- Node setup
- `npm run build`
- build非書換確認
- JS syntax check
- sender Hard Gate明示実行
- `npm run test:release-gate`
- `git diff --check`
- `npm run validate:release`

CIのPASSはcommit SHA単位。修正commitでheadが動いたら最終headに対してCIを取り直す。

merge対象head SHAとCI PASS対象SHAは同一でなければならない。

## 19. PR / squash merge

PR本文へ最低限記録する。

- 開始main SHA
- branch
- ページ全体reference_at
- sender reference_at
- source hierarchy
- 愛媛20/20
- 愛媛missing 0
- 47/47 prefecture discovery
- 全国discovered sender数
- nationwide adjudicated数
- source mention orphan 0
- CURRENT判定原則
- UNKNOWN / NO_EVIDENCE件数
- municipal/national既存監査が維持されていること
- build / self-test / Release Gate結果
- Desktop/Mobile QA予定

required checksがPASSするまでmergeしない。

mergeは`squash`。expected head SHAを固定してmergeし、merge後main SHAを再取得する。

## 20. GitHub Pages Production QA

merge後、Pages deployを確認する。

### 20.1 配信一致

以下をmerged mainと実配信で一致確認する。

- `index.html`
- main dashboard HTML
- CSS / JS / current-shelters.json
- `sender-municipalities.html`
- `sender-municipality-audit.json`
- `sender-audit/ehime.json`
- 全地域sender audit JSON
- sender source catalog
- discovery log

### 20.2 Desktop / Mobile

Desktop：約1440×1000

Mobile：約390×844

main dashboard：

- overview
- needs
- timeline
- dashboard
- volunteer
- map

sender page：

- 愛媛20/20表示
- 全国sender件数
- 47/47表示
- orphan 0表示
- 都道府県filter
- state filter
- keyword検索
- source link

Hard Gate：

- page error 0
- console error 0
- ページ全体の横スクロール事故なし
- カード崩れなし
- filter操作可能
- source data読込成功

`tablebox`等内部スクロールは許容するが、document全体がviewport外へ不意にoverflowしてはならない。

## 21. Branch Protection推奨

- mainへのPR経由を必須化
- `Build and release gate`をrequired check化
- merge前に最新mainへ追随
- 通常main直接push禁止
- 管理者bypassは緊急時のみ

設定できない場合は未設定を完了扱いにしない。

## 22. 最終チェックリスト

### 開始状態

- [ ] `origin/main`取得
- [ ] latest main SHA記録
- [ ] open PR確認
- [ ] open branch / 未マージ作業確認
- [ ] 古いSHAへresetしていない

### 既存Version 2.5 Coverage

- [ ] 熊本県内45市町村確認
- [ ] municipal-support全件再監査
- [ ] national-support全件再監査
- [ ] Institution Coverage Gate
- [ ] Source Mention Closure
- [ ] record状態と部分指標状態を分離
- [ ] `missing_entities = []`
- [ ] `unadjudicated_source_mentions = []`

### 愛媛sender

- [ ] fixed universe = 20
- [ ] adjudicated = 20
- [ ] missing = 0
- [ ] source mention orphan = 0
- [ ] blocking unresolved = 0
- [ ] UNKNOWN / NO_EVIDENCEにreason・checked_at・sources_checked・next_review_atあり
- [ ] aggregate人数を市町別へ推計配分していない

### 全国sender

- [ ] 47都道府県すべて`CHECKED`
- [ ] discovery source logあり
- [ ] 発見済み全senderを裁定
- [ ] 東京都特別区を対象に含む
- [ ] 政令市行政区・消防組合等をmunicipality分母へ誤算入していない
- [ ] entity keyは`prefecture + municipality_name`
- [ ] sender source mention orphan = 0
- [ ] secondary-only CURRENT = 0
- [ ] schedule-only CURRENT = 0
- [ ] blocking conflict = 0

### Build / Release

- [ ] source/public一致
- [ ] `npm run build` PASS
- [ ] sender validators全PASS
- [ ] 10件sender自己破壊テストPASS
- [ ] 既存Release Gate自己テストPASS
- [ ] `npm run validate:release` PASS
- [ ] `git diff --check` PASS
- [ ] temporary migration / fixer / markerなし
- [ ] final head SHAとCI PASS SHA一致
- [ ] squash merge
- [ ] merge後main SHA記録

### Pages

- [ ] Pages deploy PASS
- [ ] main dashboard配信一致
- [ ] sender UI配信一致
- [ ] sender audit JSON群配信一致
- [ ] rootからmain dashboardとsender pageへ導線あり
- [ ] Desktop main dashboard QA PASS
- [ ] Mobile main dashboard QA PASS
- [ ] Desktop sender QA PASS
- [ ] Mobile sender QA PASS
- [ ] console error 0
- [ ] page error 0
- [ ] document-level overflow 0
- [ ] filters正常
- [ ] source links正常

## 23. 禁止事項

- 支援予定期間内だからCURRENT
- 集約人数を市町別へ推計配分
- 「派遣予定」を「派遣中」へ書換え
- secondary sourceだけでCURRENT
- 愛媛20市町の一部だけ見て全件監査と称する
- 指定都市だけ見て全国基礎自治体Coverageと称する
- 既知リストだけ見て47都道府県discoveryを省略する
- source mention orphanを放置する
- UNKNOWNを消して見た目上100%確定にする
- NO_EVIDENCEを「支援なし」と断定表示する
- 同名自治体を市町村名だけで同一entity化する
- validatorを弱めてPASSさせる
- temporary fixerをmainへ残す
- Pages未確認で完了扱いする

## 24. 運用原則

目的は「今知っているsenderを増やす」ことではない。

今後のどの時点更新でも、

- 愛媛は必ず20/20を再監査する。
- 全国は必ず47都道府県discoveryを行う。
- 新たに発見した基礎自治体senderを必ず裁定する。
- 採用sourceに出ているsender municipalityが台帳にない状態をRelease Gateで止める。
- 既存Version 2.5の監査を弱めない。
- merge後の実配信まで確認する。

前回と同種の漏れを公開後の追加PRで直すのではなく、可能な限りmerge前に機械的Gateで止めることを本運用の原則とする。
