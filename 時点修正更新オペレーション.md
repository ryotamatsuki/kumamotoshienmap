# 時点修正更新オペレーション

> Version 2.1（2026-08-22改訂）
>
> Version 2.0の手順を、Release Gate・CI・候補JSON検証で機械的に止める運用へ拡張する。2026-08-21の実更新で判明した「旧時刻の残存」「原本と公開コピーの同期漏れ」「異なる定義の避難所数の混同」「Pages公開後のJavaScriptキャッシュ」等を、PRの段階で検出する。今回のVersion 2.1実装PRでは、サイトの現行データ・数値は更新しない。

## 1. 目的・対象・完了条件

熊本県への支援・受援ダッシュボードを、公式情報の対象時点を壊さずに更新し、GitHub PagesおよびSites用ビルドへ再現可能な形で反映する。

対象リポジトリ：ryotamatsuki/kumamotoshienmap

公開URL：https://ryotamatsuki.github.io/kumamotoshienmap/

完了とは、単にファイルを変更することではない。次のすべてを満たすことをいう。

1. 公式情報を再探索し、各値に根拠・対象時点・確認時刻がある。
2. 現況、履歴、予定、実績、未確認、公式情報間の相違を分離している。
3. 正規化データからHTML、JavaScript、JSON、カレンダー、地図、サマリーを同期生成している。
4. source HTML、公開用HTML、配信ビルドの不一致がない。
5. 自動検証、リンク・HTTP検証、Desktop/Mobile実機確認を通過している。
6. 最新mainから作成したPRをmainへ反映し、反映後のPages実配信とSHAを確認している。

## 2. 時点の定義

「最新」という一つの日時にすべてを合わせない。次の時点を別フィールドで管理する。

| フィールド | 意味 |
|---|---|
| reference_at | 今回の更新判断の基準日時（JST） |
| source_as_of | 公式資料が表す対象時点 |
| source_published_at | 資料の公表日時 |
| source_updated_at | 公式ページ・JSONの更新日時 |
| fetched_at | API・JSONを取得した日時 |
| checked_at | 担当者が内容を確認した日時 |
| page_checked_at | ページ全体を最後に目視確認した日時 |
| release_id | 今回の更新を識別する日時ID。例：20260822-1500 |
| deployed_sha | 公開反映後のmainコミット |

画面には、原則として対象時点と確認日時を表示する。資料の公表日時、データ取得日時、ページ確認日時を相互に置換しない。

## 3. 状態の判定規則

| 状態 | 採用条件 | 画面表示 |
|---|---|---|
| CURRENT | 基準日時点で公式情報が適用中と確認できる | 現行・活動中・募集中 |
| HISTORICAL | 期間終了、活動終了、現行継続を確認できない | 履歴・実績 |
| PLANNED | 公式に将来予定と明記、実施・実働は未確認 | 予定・実働要確認 |
| UNKNOWN | 未掲載、取得失敗、次回日程不明など | 情報未確認・要再確認 |
| CONFLICT | 公式資料同士で対象時点・集計範囲・値が異なる | 公式情報間で相違あり |
| NOT_APPLICABLE | 公式に対象外・該当なしと明記 | 対象外 |

次の変換は禁止する。

- 公式ページの削除・404を「募集なし」へ変換しない。
- 受付フォームの終了を「定員到達」へ変換しない。
- 予定人数を実参加人数へ変換しない。
- 個人募集を団体受入れ可へ拡張しない。
- 県報の「開設避難所数」とライブJSONの「現在開設施設数」を同じ値として扱わない。
- 施設別人数が公表されていない場合に推計値を表示しない。
- 最新資料で継続確認できない過去の対口支援・派遣を、現行と断定しない。

## 4. リポジトリの役割分担

| 種類 | ファイル | 役割 | 更新方法 |
|---|---|---|---|
| GitHub Pages入口 | index.html | ルートから本体HTMLへ遷移 | 通常は変更しない |
| Pages本体・正本 | ehime_kumamoto_support_geocoded_shelters_20260802.html | レビュー済みの正本 | 原則ここだけを編集 |
| Sites用公開コピー | public/dashboard.html | 正本と同一に保つコピー | 手編集せず生成・同期 |
| 現在避難所 | current-shelters.json | 公式ライブJSONから生成した現況 | update-current-shelters.mjs |
| 避難所の位置履歴 | HTML内の座標マスター、shelter-coordinate-manifest.json | 206施設等の位置スナップショット | 現行リストと混在させない |
| ボランティア調査 | research_official_north.json、research_official_south.json、research_official_statewide.json | 公式調査の正規化前データ | 調査packetから更新 |
| ボランティア生成物 | volunteer-data.js | UIが参照する単一データ | generate-volunteer-data.mjs |
| UI | volunteer.js、volunteer.css | ボランティア表示・操作 | 機能変更時のみ |
| 検証 | scripts/validate-*.mjs | データ・UI・ビルドの品質ゲート | 期待値変更時も確認 |
| Sitesビルド | scripts/build-sites.mjs、dist/ | 配信用Worker・成果物 | npm run buildで生成 |
| 運用文書 | 時点修正更新オペレーション.md | 本手順 | 実作業で漏れが判明したら更新 |

scripts/update-*202608*.mjs、scripts/fix-*202608*.mjs、scripts/cleanup-*202608*.mjsは特定日時の作業履歴である。次回更新でそのまま再実行してはならない。再利用する場合は、対象時点・URL・置換値・履歴化条件を全件レビューする。

GitHub PagesとSitesは別経路である。distの検証に合格しただけでGitHub Pagesの公開確認済みとはしない。

## 5. 更新開始前の固定

### 5.1 最新mainと作業ブランチ

開始時に、古いSHAへ戻さず、その時点の最新mainを基準にする。

ローカルGitの場合：

~~~sh
git fetch origin
git status --short
git log -1 --oneline --decorate origin/main
git switch -c audit/timepoint-YYYYMMDD-HHMM origin/main
~~~

GitHub連携の場合：

1. default branchがmainであることを確認する。
2. mainの最新commit SHAを取得し、台帳へ記録する。
3. そのSHAから作業ブランチを作成する。
4. 同一パスへの更新APIを並列実行しない。
5. 直接mainへ更新せず、PRを作成してからmergeする。

未コミット変更を無断で破棄しない。既存変更がある場合は、対象ファイルと所有者を確認する。

### 5.2 更新台帳

実更新PRでは、作業開始時に機械可読な`UPDATE_LEDGER.json`を作る。説明用の雛形は`UPDATE_LEDGER.example.json`、正式なschemaは`scripts/update-ledger.schema.json`である。台帳を毎回mainへ残すかは運用で決めてよいが、実更新PRのCIへ渡す台帳は、差分と同じブランチ上のファイルにする。

次の項目を必須とする。

~~~json
{
  "schema_version": 1,
  "repository": "ryotamatsuki/kumamotoshienmap",
  "update_type": "timepoint_refresh",
  "gate_phase": "pre_merge",
  "update_id": "refresh-YYYYMMDD-HHMM",
  "release_id": "YYYYMMDD-HHMM",
  "base_main_sha": "開始時に取得したSHA",
  "reference_at": "2026-08-22T15:00:00+09:00",
  "page_checked_at": null,
  "expected_changed_files": [],
  "sources": [],
  "coverage": {
    "statewide": "pending",
    "municipalities": "45市町をrechecked/not_applicable/unknownへ分類",
    "researched_centers": "対象市町を全件再確認"
  },
  "snapshots": {},
  "unresolved": []
}
~~~

`base_main_sha`は開始時に解決したmain SHAと一致させる。`expected_changed_files`は、実際にstageまたはPR差分へ含めるPOSIX相対パスの完全な集合とし、不足・想定外・重複を許容しない。`page_checked_at`はpre-mergeではnullでもよいが、post-deploy台帳では必須とする。`unresolved`が空でない台帳はRelease GateでFAILにする。

調査行には、page_section、record_id、owner、old_value、new_value、status、source_ids、current_or_history、checked_at、reviewerを持たせる。変更がない市町・ページも「再確認済み・変更なし」と記録する。未処理行を残したまま実装へ進まない。

台帳は次のように検証する。

~~~sh
# stage済み差分を検証
node scripts/validate-update-release.mjs --ledger UPDATE_LEDGER.json --mode staged

# PRのbaseからHEADまでを検証
node scripts/validate-update-release.mjs --ledger UPDATE_LEDGER.json \
  --mode base --base "$PR_BASE_SHA" --head HEAD
~~~

`--ledger path`と`--ledger=path`、`--base path`と`--base=path`の両形式を利用できる。未追跡ファイルが残っている場合もFAILとする。

### 5.3 変更対象レジストリ

最低限、次の対象を台帳に列挙する。

- 県全体KPI、被害、避難者、断水、給水、道路、廃棄物
- 国・関係機関の応援人数、救助、TEC-FORCE等
- 愛媛県の支援（現行、終了、予定、実働要確認）
- 市町別の支援ニーズと対口支援
- 災害ボランティア全45市町村の募集状態、対象地域、団体可否、人数公表、車両ニーズ
- 現在開設避難所JSON、座標、件数、取得時刻
- 206施設等の位置履歴と座標マニフェスト
- RECORDS、IMPACTS、NEED_MUNICIPALITIES、TIMELINE_EVENTS、SOURCES
- PAGE_RECHECK_META
- currentCalendarOverridesと日付付き現行補正ブロック
- HTML内のvolunteer-data.jsのクエリ
- public/dashboard.html、volunteer-data.js、dist/
- 時点依存validatorの期待値・現況メタデータ
- 構造・品質validatorの座標・current shelters整合
- Release Gateの台帳、差分、parity、release_id検査
- 時点修正更新オペレーション.md

## 6. エージェント分担

同じファイルを複数エージェントが編集しない。研究と実装を分離し、親がfan-inして差分を裁定する。

| 役割 | 調査対象 | 書き込み範囲 |
|---|---|---|
| 県全体 | 熊本県・国のKPI、被害、避難、断水、応援 | 調査packet |
| 市町・災害VC | 45市町の募集、団体、地域、日程、定員 | research_official_*.jsonの候補 |
| 愛媛支援 | 愛媛県の派遣・物的支援・予定・実績 | 調査packet |
| 避難所・座標 | 公式JSON、開設判定、座標突合、206履歴 | 調査報告 |
| コード監査 | DOM、タブ、パス、データ参照、validator | 読み取り専用 |
| 正規化 | 旧値・新値、current/history、重複、時点差 | 統合ブランチのみ |
| 公開QA | HTTP、Pages、Desktop/Mobile、キャッシュ、リンク | 読み取り専用 |
| 親 | 時点固定、allowlist、差分裁定、release gate | 全体統合 |

各調査packetには、担当、範囲、URL、資料名、対象時点、原文、正規化値、旧値、新値、状態、未解決事項、確認時刻、証跡ハッシュを必須とする。

## 7. 公式情報の再探索と時点マトリクス

### 7.1 情報源の優先順位

1. 熊本県・市町・社会福祉協議会等の公式ページ、公式PDF、公式JSON。
2. 愛媛県、国土交通省、消防庁、防衛省、内閣府等の公式資料。
3. 公式サイト内の公式SNS・公式フォーム。
4. 二次報道は発見の手がかりに限定し、最終値の根拠にしない。

ページの更新日だけでなく、本文、対象日、活動日、終了・休止、定員到達、申込条件、対象地域を読む。取得失敗はUNKNOWNとし、前回値を現況へ自動継承しない。

### 7.2 情報源別の基準

| データ群 | 現況の基準 | 履歴・比較の基準 | 混同禁止 |
|---|---|---|---|
| 現在開設避難所 | 熊本県公式JSONの開始日時あり・終了日時なし | 公式JSONの過去スナップショット、座標マスター | 県報の速報件数 |
| 県の被害・避難KPI | 最新の県報が表す対象時点 | 過去の第何報か | 現在JSONの施設件数 |
| ボランティア | 各センターの最新公式告知 | 終了した募集・過去日程 | フォーム終了と定員到達 |
| 愛媛県支援 | 最新資料で継続・予定が確認できるもの | 終了班・過去派遣 | 予定を実働と断定 |
| 対口支援 | 最新資料で継続確認できるもの | 配置履歴のみの古い資料 | 古い配置表を現行化 |
| 座標 | 公式JSONの座標を優先し、既存マスターで突合 | 206施設等の位置履歴 | 推測座標の追加 |

県報の69か所と公式JSONの現在71施設が異なる場合、どちらかを誤りとして消さない。前者を県報の対象時点の速報、後者を公式JSONによる現在施設一覧として、両方の定義・時点・出典を表示する。

### 7.3 市町・災害VCの全件確認

県内45市町村を、次のいずれかへ必ず分類する。

- rechecked_current
- rechecked_no_change
- not_applicable
- official_not_found
- unknown_needs_recheck
- conflict

対象となる市町の詳細調査は、募集日、活動日、個人・団体、県外可、愛媛県可、人数、車両、フォーム、連絡先、注意事項を再確認する。明示されていない項目はnullまたは要照会とし、推定しない。


### 7.4 状態軸を分離する

自由文のstatusだけで現況を判定しない。最低限、次の軸を台帳・正規化データ・画面で対応させる。

| 軸 | 例 | 意味 |
|---|---|---|
| view_state | current / history | 現行表示か履歴表示か |
| fact_type | snapshot / planned / actual | スナップショット、予定、実績 |
| verification_state | confirmed / unconfirmed / unavailable / conflict | 根拠の確認状態 |
| review_state | ok / due / overdue / blocked | 再確認の必要性 |
| recruitment_state | recruiting / closed_confirmed / paused / preregistration / not_published / unknown | 募集状態 |
| aggregation_level | prefecture_total / municipality / facility | 県計、市町、施設の集計単位 |
| aggregation_basis | official_total / municipality_sum / current_json / coordinate_master | 数値の定義・根拠 |

review_state=dueは募集状態ではない。たとえば「必要人数到達で受付終了、8月24日以降の再告知は未確認」は、closed_confirmedかつreview_state=dueとして扱う。フォームが閉じているだけならclosed_confirmedへ遷移させない。

予定は日付が過ぎただけで実績・終了へ自動変換しない。公式に実施・終了・中止が確認できるまで、plannedとreview_state=dueまたはoverdueを維持する。

## 8. 実装の正しい順序

### Phase A：調査packetと正規化

1. 公式資料を対象時点付きで登録する。
2. 更新台帳の旧値・新値・状態・出典を埋める。
3. current/history/planned/unknown/conflictを裁定する。
4. 現行値を表示するページ、履歴へ移すページ、変更なしのページを一覧化する。
5. 1つのソースを正規化データへ反映し、HTMLへ同じ値を手入力で重複させない。

### Phase B：現行避難所の更新

公式JSONを更新する場合は、まずdry-runする。

~~~sh
npm run update:current-shelters -- --dry-run \
  --output /tmp/current-shelters-candidate.json
node scripts/validate-current-shelters.mjs \
  --candidate=/tmp/current-shelters-candidate.json
~~~

dry-runは候補JSONを書き出す。候補の取得時刻、Last-Modified、現在件数、座標状態、未解決、競合を、既存の`current-shelters.json`ではなく候補そのものへvalidatorを投入して確認する。本番`current-shelters.json`は変更されない。候補validatorがPASSした後の昇格・レビューを経て、必要な場合だけ実更新する。

~~~sh
npm run update:current-shelters
npm run generate:current-shelter-data
npm run validate:current-shelters
~~~

原則：

- 開設日時あり・終了日時なしを現行候補とする。
- 公式JSONの座標を優先する。
- 206件の既存座標マスターは突合用であり、現行件数へ足し戻さない。
- 公式座標と履歴座標の100m超差分は自動採用せず、conflictまたは要確認にする。
- 座標のない施設を推測して追加しない。
- current-shelters.jsonのmeta.current_countと配列件数を一致させる。

通常のPR CIでは公式JSONをlive取得しない。外部取得はローカルの更新作業または`workflow_dispatch`等の更新専用実行に分離し、CIはcommit済みfixtureと候補検証結果を再現可能に検査する。

### Phase C：正本HTMLと公開コピー

1. 編集前に正本HTMLとpublic/dashboard.htmlを一致させる。
2. 正本はehime_kumamoto_support_geocoded_shelters_20260802.htmlだけを編集する。
3. RECORDS、IMPACTS、NEED_MUNICIPALITIES、TIMELINE_EVENTS、SOURCES、PAGE_RECHECK_METAを台帳どおり更新する。
4. 旧値を削除する前に、履歴または対象時点の古い資料として残す。
5. 正本を公開コピーへ同期する。
6. generate-current-shelter-data.mjsで現行避難所ブロックを両方へ生成する。
7. バイト一致を確認する。

~~~sh
cp ehime_kumamoto_support_geocoded_shelters_20260802.html public/dashboard.html
npm run generate:current-shelter-data
cmp --silent ehime_kumamoto_support_geocoded_shelters_20260802.html public/dashboard.html
~~~

### Phase D：ボランティア生成と現行カレンダー

調査JSONを更新した後に、必ず生成する。

~~~sh
npm run generate:volunteer
npm run validate:volunteer
~~~

次を忘れない。

- currentCalendarOverridesと日付付き現行補正を新基準日時へ更新する。
- 過去スナップショット用のcalendarOverridesを現行情報で上書きしない。
- recruiting、full、paused、planned、unknownの意味を維持する。
- 受付終了フォームを現行申込導線に残さない。
- needs_reconfirmation、eligibility_currently_applicable、recheck_statusを各対象市町で確認する。
- 生成後のmeta.reference_at、meta.checked_at、最古確認時刻、公式ソース数を確認する。

### Phase E：確認時刻とキャッシュバスター

今回のreference_atとrelease_idを先に固定する。volunteer-data.jsを更新したら、HTMLのすべての参照を同じrelease_idへ更新する。

~~~sh
rg -n "volunteer-data\\.js" ehime_kumamoto_support_geocoded_shelters_20260802.html public/dashboard.html
rg -n "reference_at|checked_at|PAGE_RECHECK_META|2026-" ehime_kumamoto_support_geocoded_shelters_20260802.html volunteer-data.js
~~~

次を満たす。

- 旧時刻が現行表示、サマリー、カード、カレンダー、地図説明に残っていない。
- 旧時刻が履歴に残る場合は、履歴であることが明示されている。
- HTMLのvolunteer-data.jsのクエリと台帳のrelease_idが一致する。
- public/dashboard.htmlにも同じクエリがある。
- 公開後は新規タブ、ハードリロード、可能ならプライベートウィンドウで再確認する。

### Phase F：validatorの責務別検証

validatorは本数で数えず、責務で分類する。既存validatorを削除・弱体化してテストを通してはならない。

| 分類 | 実在するvalidator | 役割 |
|---|---|---|
| 時点依存 | `validate-dashboard-current.mjs`、`validate-current-state-audit.mjs`、`validate-volunteer-data.mjs`、`validate-built-site.mjs` | 現況日時、県報値、募集状態、生成サイトの現行期待値 |
| 構造・品質 | `validate-shelter-coordinates.mjs`、`validate-current-shelters.mjs` | 206点位置履歴、current sheltersのschema・ID・自治体・座標・conflict |
| Release Gate | `validate-update-release.mjs` | 台帳schema、base SHA、差分集合、release_id、current metadata、source/public/dist parity |

通常のbuildと時点修正PRのRelease validationは分離する。`npm run build`は既存の生成・validator・Sitesビルドを維持し、`npm run validate:release`はbuild後の統合ゲートとして実行する。

次回の基準日時が変わったら、次のファイルを台帳と同じ値へ更新する。

- scripts/validate-dashboard-current.mjs
- scripts/validate-current-state-audit.mjs
- scripts/validate-volunteer-data.mjs
- scripts/validate-built-site.mjs

更新対象の例：

- checkedAt、reference_at
- 県報の対象日・KPI
- 公式JSONのfetched_at、source_last_modified、現行件数
- 現行募集市町数、県外参加明示数、団体参加経路数
- キャッシュバスター文字列
- 禁止された旧URL・旧現況文言

期待値を削除してテストを通すことは禁止する。日付付きの`update-*202608*.mjs`、`fix-*202608*.mjs`、`cleanup-*202608*.mjs`はhistorical migration scriptsであり、通常の更新コマンドでは実行しない。将来、時点依存validatorの期待値を台帳やexpected-current.jsonから読むように改修できるが、それまでは日時変更とvalidator更新を同一PRに含める。


### Phase F.5：Release Gateによる変更ファイルの漏れ防止

作業開始時の変更対象レジストリを、commit前・PR時の機械的なゲートにする。固定のgit add一覧だけに依存しない。通常の時点修正で必ず全ファイルを変更するという固定ルールにもせず、更新ごとの`expected_changed_files`を正とする。

~~~sh
git status --short
git diff --name-only
git diff --cached --name-only
~~~

台帳の`expected_changed_files`と、実際の差分集合が一致することを確認する。

~~~sh
# ローカルstage済み差分
node scripts/validate-update-release.mjs --ledger UPDATE_LEDGER.json --mode staged

# PRのbaseからHEADまで（CIと同じ検査）
node scripts/validate-update-release.mjs --ledger UPDATE_LEDGER.json \
  --mode base --base "$PR_BASE_SHA" --head HEAD
~~~

不足ファイル、想定外ファイル、未追跡ファイルがある場合はFAILする。`dist/`は通常Git差分へ含めず、`npm run build`後にRelease GateがdistのHTML、CSS、JS、JSON、Sites Workerへ同じ内容が埋め込まれているか確認する。`package-lock.json`を例外扱いしない。lockfileを追加する場合は台帳へ明示する。通常のデータ更新で候補になるファイルは、少なくとも次のとおりである。

- current-shelters.json
- ehime_kumamoto_support_geocoded_shelters_20260802.html
- public/dashboard.html
- volunteer-data.js
- research_official_north.json
- research_official_south.json
- research_official_statewide.json
- scripts/validate-dashboard-current.mjs
- scripts/validate-current-state-audit.mjs
- scripts/validate-current-shelters.mjs
- scripts/validate-volunteer-data.mjs
- 時点修正更新オペレーション.md

実際に変更していないファイルを形式的にcommitしない。逆に、current-shelters.json、座標マニフェスト、生成スクリプト、validatorを変更した場合に一覧から落とさない。

### Phase G：ビルド

canonicalな実行は次のとおりである。

~~~sh
npm run build
~~~

package.jsonのbuildは、現行避難所データのHTML埋込生成、ボランティア生成、dashboard/current-state/volunteer/shelter validator、Sitesビルド、配信検証を順に実行する。公式JSONのlive取得は含まれない。個別に切り分ける場合は次を使う。

~~~sh
npm run validate:dashboard
npm run validate:current-state
npm run validate:volunteer
npm run validate:shelters
npm run validate:current-shelters
node --check volunteer.js
node --check volunteer-data.js
node --check scripts/build-sites.mjs
node scripts/build-sites.mjs
npm run validate:dist
git diff --check
~~~

npm run buildが旧日時の不一致で失敗した場合、テストを削除せず、正しい時点へ期待値を更新する。public/dashboard.htmlと正本が一致しない場合は、正本から再同期する。

### Phase H：Release Gate

build後、時点修正PRでは次の統合ゲートを必ず実行する。

~~~sh
# 台帳を使う実更新PR（ローカルstage済み）
npm run validate:release -- --ledger=UPDATE_LEDGER.json --staged

# 台帳を使う実更新PR（PR base差分）
npm run validate:release -- --ledger=UPDATE_LEDGER.json \
  --base="$PR_BASE_SHA" --head=HEAD --require-ledger-if-data-change

# Release Gate実装・CI・文書だけのcontrol-plane PR
npm run validate:release -- --base="$PR_BASE_SHA" \
  --require-ledger-if-data-change
~~~

`--ledger`、`--base`、`--head`は空白区切り・`=`形式の両方を受け付ける。`--mode staged`、`--mode head`、`--mode base`は相互排他的である。CIは`.github/workflows/validate-update.yml`から同じvalidatorを呼ぶ。

Release Gateの自動FAIL条件は次のとおりである。

- UPDATE_LEDGERのschema、必須フィールド、release_id、base_main_sha、unresolved
- expected_changed_filesとstaged/PR差分の不足・想定外・重複
- 未追跡ファイル
- `PAGE_RECHECK_META`、`VOLUNTEER_DATA.meta`、`CURRENT_SHELTER_META`、`current-shelters.json`の現況metadata不一致
- source HTMLと`public/dashboard.html`のバイト不一致
- `dist/dashboard.html`、distのCSS/JS/JSON、Sites Workerの古い埋込内容
- HTML・public・dist・Worker・任意のvolunteer metadataにおけるrelease_id/cache-buster不一致
- current_count、配列件数、coordinate conflict、unresolved、facility ID、自治体、座標出典の不整合
- build、既存validator、JavaScript構文、`git diff --check`の失敗

過去の日時が存在するだけではFAILにしない。タイムライン・履歴・過去スナップショットは履歴ラベルがあれば許容し、現況metadataだけを台帳・相互参照で検査する。

### Phase I：PR CI

`.github/workflows/validate-update.yml`はmain向け`pull_request`と`workflow_dispatch`で動作する。Node.js 22.x、lockfileがない現状では`npm install --ignore-scripts --no-audit --no-fund --package-lock=false`を使い、`npm run build`、JavaScript構文、tracked/untracked差分、`git diff --check`、Release Gateを実行する。通常PR CIでは`npm run update:current-shelters`を実行せず、公式JSONのlive fetchを行わない。

現況データ・HTML・研究JSON・`volunteer-data.js`等を変更するtimepoint refresh PRは`UPDATE_LEDGER.json`を必須とし、control-plane PRでは台帳を任意とする。データパスが差分に含まれるのに台帳がない場合はvalidatorがFAILする。Actionsからmainへの自動commit・mergeは行わない。

## 9. 自動QAと漏れ検出

### 9.1 データ整合

- current-shelters.jsonの配列件数とmeta.current_countが一致する。
- 現行公式JSONの件数、県報の速報件数、位置履歴件数が別々に表示される。
- 現行避難所の座標未解決・競合が0、または未解決理由が明示されている。
- 45市町村の確認状態に未処理がない。
- 研究対象市町の必須フィールドが全てあり、空欄を0へ変換していない。
- 公式ソースURL、資料名、対象時点、確認時刻が各カード・レコードから辿れる。
- CURRENTとHISTORICALが同じサマリー・集計へ二重計上されていない。

### 9.2 故障注入テスト

Release Gateを単なる文字列検索にしないため、更新PR前に一時コピーで次を実行する。Case 1〜6は意図した異常をvalidatorがFAILさせること、Case 7は履歴日付を誤検出せずPASSすることを確認する。

| Case | 故障注入 | 期待結果 |
|---|---|---|
| 1 | 正本HTMLだけ変更し、`public/dashboard.html`を古いままにする | Release Gate FAIL（source/public parity） |
| 2 | `volunteer-data.js`のrelease_idを新しくし、HTML queryを旧値のままにする | Release Gate FAIL（release_id） |
| 3 | 台帳のexpected_changed_filesに`current-shelters.json`を入れるが差分から除外する | Release Gate FAIL（changed files） |
| 4 | candidateの`meta.current_count`と配列件数をずらす | candidate validator FAIL（count） |
| 5 | candidateへ100m超の座標差を混入する | candidate validator FAIL（coordinate conflict） |
| 6 | 台帳または現況metadataのreference_atを旧値にする | Release Gate FAIL（current metadata） |
| 7 | 履歴データへ古い日付を残す | 現況metadataが整合していればPASS |

故障注入でファイルを壊した一時コピーはmainへcommitしない。実施結果はPR本文へ、意図した異常をFAILさせたという意味のPASS/FAILとして記録する。

### 9.3 旧時点・重複表示の監査

今回の基準日時より前の文字列が見つかったら、直ちに削除しない。現行表示か履歴かを判定し、現行なら更新、履歴ならラベルと根拠を確認する。

~~~sh
rg -n "2026-|checkedAt|reference_at|source_as_of|source_last_modified|PAGE_RECHECK_META|currentCount|latestCount|募集|受付終了|要再確認" \
  ehime_kumamoto_support_geocoded_shelters_20260802.html \
  public/dashboard.html volunteer-data.js current-shelters.json \
  research_official_*.json scripts
~~~

必ず説明できる状態にする差分：

- 同じ画面に異なる確認時刻が残る。
- 県報の件数とライブJSONの件数が同じラベルで表示される。
- 予定日を過ぎた募集が募集中のまま残る。
- 受付終了フォームが現行ボタンとして残る。
- 正本にはあるがpublic/dashboard.htmlにない。
- volunteer-data.jsを更新したのにHTMLのクエリが旧時点のまま。
- distが前回のデータを埋め込んでいる。

### 9.4 URL・配信

- 公式URLを全件監査する。
- root、本体HTML、volunteer.css、volunteer.js、volunteer-data.js、current-shelters.jsonのGET/HEADを確認する。
- 404を募集なしへ変換していない。
- 405のメソッド制御と404ルートをvalidate:distで確認する。
- index.htmlが正しい本体HTMLを参照する。
- 外部CDN、地図タイル、公式リンクの失敗をコンソールで確認する。

## 10. Pages公開後の実機QA

自動テストだけで公開確認済みとはしない。公開URLを新規に開いて確認する。

1. GitHub Pagesの設定と公開状態を確認する。現行リポジトリに.github/workflowsがない場合、Actions成功を前提にしない。
2. root URLと本体HTML URLがHTTP 200であることを確認する。
3. Desktopで新規タブを開き、必要ならハードリロードする。
4. Mobile viewportでも同じ確認をする。
5. 次の6タブを順に操作する。
   - 概要
   - 支援ニーズ見通し
   - 発災後タイムライン
   - 支援ダッシュボード
   - 災害ボランティア
   - 地図
6. 各タブで、基準日時、対象時点、主要数値、現行/履歴ラベル、出典リンクを確認する。
7. 災害ボランティアで、サマリー、詳細、比較表、カレンダー、地図、受付終了・要再確認表示を確認する。
8. 公式リンク、タブ切替、フィルター、ソート、地図レイヤー、キーボード操作、レスポンシブ表示を確認する。
9. Console error、空白地図、404アセット、古い時刻・古い募集状態がないことを確認する。
10. volunteer-data.js?release_idが新しい値を読み、公開データのmeta.checked_atが今回の確認時刻であることを確認する。

ブラウザの既存タブに旧画面が残っていた場合、それをサイト障害と断定しない。一方、キャッシュで説明できると決めつけず、実配信HTML・JS・JSONの内容を取得して照合する。

## 11. 障害切り分け

| 症状 | 最初に確認するもの | 対応 |
|---|---|---|
| source/public不一致 | 正本編集後の同期 | 正本から再生成 |
| buildが旧日時で失敗 | validatorの期待値、生成データのchecked_at | 台帳と一致させる |
| 現行件数が県報と違う | 対象時点・定義・出典 | 両方を別定義で表示 |
| 座標競合 | 公式座標、ID、住所、距離 | conflictとして確認。推測しない |
| 旧時刻が画面に残る | HTMLの現行表示、補正ブロック、生成物 | 現行だけ更新し履歴は保持 |
| ボランティアが更新されない | volunteer-data.js、scriptクエリ、dist | 生成→クエリ更新→build→実配信照合 |
| rootだけ404 | index.html、公開ディレクトリ | 入口・Pages設定を確認 |
| HTMLは表示、CSS/JSが404 | 相対パス、ファイル名 | 公開URLから各アセットをGET |
| Pagesが旧画面 | Actions、main SHA、配信内容、キャッシュ | 反映待ち後、新規タブで確認 |
| 地図だけ空白 | Leaflet、タイル、レイヤー、Console | 公開アセットとCDNを確認 |
| 公式取得失敗 | 公式PDF・公式ポータル | UNKNOWNとして理由を記録 |
| 同一エラーが3回続く | エラー、SHA、ログ、権限 | 作業を止めて報告 |

## 12. PR・main・Pages反映

GitHubのdefault branchがmainでも、直接mainへpushしない。

1. 最新main SHAから作業ブランチを作成する。
2. 更新対象を明示して編集する。git add .は使わない。
3. npm run build、差分、リンク、UI確認を行う。
4. 作業ブランチへcommitする。
5. base=main、head=作業ブランチのPRを作成する。
6. PRの変更ファイル、blob SHA、検証結果、未解決事項を確認する。
7. CI/Pagesのチェックが成功したことを確認する。Actionsがない場合は、その事実を記録する。
8. head SHAを固定してmergeする。
9. merge後のmain SHAを取得する。
10. Pages反映後、公開HTML・JS・JSONの実配信をローカル検証結果と照合する。
11. Desktop/Mobileの6タブを再確認する。

PR本文には、基準日時、主要資料の対象時点、定義差、変更ファイル、生成・validator・build結果、source/public/dist一致、release_id、Pages公開後のHTTP・ブラウザ確認、未確認事項を含める。

## 13. 最終ゲート

### 根拠・時点

- [ ] 最新main SHAを開始時に記録した
- [ ] 公式情報源のallowlistと再探索範囲を記録した
- [ ] reference_at、source_as_of、fetched_at、checked_at、page_checked_atを区別した
- [ ] CURRENT/HISTORICAL/PLANNED/UNKNOWN/CONFLICTを全対象で判定した
- [ ] 45市町村の確認状態に未処理がない
- [ ] 県報件数、ライブJSON件数、位置履歴件数を別定義で保持した
- [ ] 未確認・取得失敗・フォーム終了を募集なしや0へ変換していない

### データ・生成

- [ ] current-shelters.jsonと公式JSONの現行判定が一致する
- [ ] 座標の未解決・競合を確認した
- [ ] 正規化データから全画面を更新した
- [ ] currentCalendarOverridesと日付付き現行補正を更新・レビューした
- [ ] 時点依存validatorの期待値を新時点へ更新した
- [ ] 構造・品質validator（座標、current shelters）を実行した
- [ ] Release Gateを台帳・差分モード付きで実行した
- [ ] expected_changed_filesと実差分が完全一致した
- [ ] 台帳のbase_main_shaと検証対象base SHAが一致した
- [ ] unresolved/conflict/current_count不整合がない
- [ ] release_idとcache-busterがHTML・public・dist・Workerで一致した
- [ ] volunteer-data.jsを再生成した
- [ ] volunteer-data.jsのクエリをrelease_idへ更新した
- [ ] source HTMLとpublic/dashboard.htmlがバイト一致する
- [ ] distが今回のデータを埋め込む
- [ ] distのHTML、CSS、JS、JSON、Sites Workerが生成元と一致する
- [ ] source HTML、public/dashboard.html、dist/dashboard.html、Pages実配信HTMLの対応を確認した

### QA・公開

- [ ] npm run buildが成功した
- [ ] `.github/workflows/validate-update.yml`のPR CIが実行対象になっている
- [ ] git diff --checkと静的JavaScript構文確認が成功した
- [ ] 公式URL・主要アセットのGET/HEADを確認した
- [ ] GitHub Pagesのroot・本体HTML・全アセットを確認した
- [ ] Desktop/Mobileで6タブ、リンク、Console、地図、キャッシュを確認した
- [ ] 公開実配信のHTML/JS/JSONが検証済み内容と一致した
- [ ] merge後main SHAとPages反映を記録した
- [ ] 未確認事項と次回レビュー対象を明記した

## 14. 標準完了報告

1. 対象リポジトリ、PR、merge commit、最終main SHA
2. reference_at、page_checked_at、release_id
3. 再探索した公式情報源の範囲
4. 県報・公式JSON・履歴データの件数と定義
5. 登録・更新した市町数、全45市町の確認状態
6. 現行から履歴へ移した項目、予定・実績・未確認の分離
7. 主要な未解決事項、要再確認事項、公式情報間の相違
8. 変更した主要ファイル
9. 実行した生成・validator・buildの結果
10. source/public/distの一致結果
11. PagesのHTTP、Actions、公開実配信のSHA
12. Desktop/Mobile実機確認、6タブ、Console、キャッシュ監査結果

確認できない事実を完了報告で補わない。実ブラウザまたは公開実配信を確認できない場合は未確認と明記し、公開済みとは報告しない。

## 15. 前回実作業からの再発防止事項

- 旧確認時刻が画面の一部に残ったため、現行表示・履歴・validator・生成物を一括監査する。
- source HTMLを修正してもpublic/dashboard.htmlが古いままになるため、バイト一致を必須ゲートにする。
- 県報の避難所数と公式JSONの現在施設数が異なるため、資料の対象時点と定義を必ず併記する。
- Pages反映後にブラウザが旧JavaScriptを読み込んだため、データ更新時はscriptクエリを更新し、実配信アセットも取得する。
- 現行・履歴・予定・実働要確認を分けないと、過去の派遣や募集が現在形で表示されるため、台帳の状態を画面へ引き継ぐ。
- 1回の更新で完了とせず、PR反映後にmain SHA、Pages、HTTP、Desktop/Mobile、6タブを再確認する。
- GitHub PagesとSitesの検証結果を混同しない。Actionsがない場合は、その事実を完了報告へ記録する。

## 16. Version 2.1で追加した機械的防止機構

- `UPDATE_LEDGER.example.json`と`scripts/update-ledger.schema.json`で台帳を機械可読化した。
- `scripts/validate-update-release.mjs`をRelease Gateとして追加し、台帳schema、base SHA、changed files、release_id、現況metadata、source/public/dist parity、current shelters整合を検証する。
- `update-current-shelters.mjs --dry-run --output`で候補JSONを保存し、`validate-current-shelters.mjs --candidate`へ直接投入できるようにした。
- `npm run build`と`npm run validate:release`を分離し、既存buildの上にRelease Gateを追加した。
- `.github/workflows/validate-update.yml`でPR時のbuild、syntax、diff、Release Gateを必須化した。通常PR CIは公式live fetchを行わない。
- 旧日時の履歴保持を許容しつつ、現況metadataだけを相互照合する設計にした。

自動化できない残存リスクは、公式情報の意味解釈、PLANNEDからACTUALへの裁定、404と募集終了の区別、公式情報間の集計定義の解釈、公開後のDesktop/Mobile目視確認である。これらは台帳・PR本文・公開後QAへ残す。
