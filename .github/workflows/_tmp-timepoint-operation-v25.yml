name: Temporary timepoint operation v2.5 updater

on:
  pull_request:
    branches: [main]

permissions:
  contents: write

jobs:
  update-doc:
    if: github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          fetch-depth: 0
      - name: Update timepoint operation to v2.5
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path

          path = Path('時点修正更新オペレーション.md')
          text = path.read_text(encoding='utf-8')
          if '> Version 2.5（2026-08-25改訂）' in text:
              print('already updated')
              raise SystemExit(0)

          def replace_once(old, new):
              global text
              if old not in text:
                  raise SystemExit(f'marker not found: {old[:80]}')
              text = text.replace(old, new, 1)

          replace_once(
              '> Version 2.4（2026-08-24改訂）',
              '> Version 2.5（2026-08-25改訂）',
          )
          replace_once(
              '> Version 2.3までのRelease Gate、対口支援・他自治体支援監査、国・関係機関支援監査に加え、**機関カバレッジ監査（Institution Coverage Gate）**を必須化する。',
              '> Version 2.4までのRelease Gate、対口支援・他自治体支援監査、国・関係機関支援監査、**機関カバレッジ監査（Institution Coverage Gate）**に加え、**一次資料中の支援主体言及まで閉包するSource Mention Closure、record状態と部分指標状態の分離、同一reference_at追加再裁定、final-head CI / Pages smoke同期**を必須化する。',
          )
          replace_once(
              '> 固定リストだけ、現在画面に出ているレコードだけ、前回監査に載った機関だけのいずれか一つを対象集合としてはならない。固定最低確認先、現行ダッシュボード、前回監査、最新の政府・熊本県等の横断資料から機関集合を作り、**「確認すべき機関集合 − 裁定済み機関集合 = 0件」**を確認してから時点修正を完了する。',
              '> 固定リストだけ、現在画面に出ているレコードだけ、前回監査に載った機関だけのいずれか一つを対象集合としてはならない。固定最低確認先、現行ダッシュボード、前回監査、最新の政府・熊本県等の横断資料、さらに**各一次資料本文・派遣元公式ページ・全国組織公式ページに登場する支援主体**まで機関集合へ取り込み、**「確認すべき機関集合 − 裁定済み機関集合 = 0件」かつ「一次資料中の支援主体言及 − 裁定済み機関集合 = 0件」**を確認してから時点修正を完了する。\n>\n> 同じreference_atに追加一次情報が見つかった場合は、reference_atを動かさずrechecked_atを追加し、既存の全件再監査証跡を消さずに再裁定する。表示文言を変更した場合はsync / validator / self-test / post-deploy smokeの期待値も同じPRで同期し、**最終head SHAに対する通常CI PASSとPages実配信QA PASS**を完了条件とする。',
          )

          replace_once(
              '- 新しい公表がないことだけを根拠にHISTORICALへ移さない。',
              '- 新しい公表がないことだけを根拠にHISTORICALへ移さない。\n- **record全体の状態と部分指標の状態を混同しない。** 活動継続をCURRENTで確認できても、全国総数・県全体集計・現金交付完了等の一部指標だけUNKNOWNであることは許容する。逆に、一部の数値だけ最新でも活動主体そのものの継続が確認できなければrecord全体をCURRENTへ推定昇格しない。\n- 一次資料で活動主体を新たに発見した場合、既存record IDに対応しないことを理由に監査対象外へ落とさない。必ずrequired_entitiesへ追加し、表示採否とは別に裁定する。',
          )

          replace_once(
              '    "official_discovery_count": 0,\n    "required_entity_count": 0,',
              '    "official_discovery_count": 0,\n    "source_mention_count": 0,\n    "source_mention_adjudicated_count": 0,\n    "unadjudicated_source_mentions": [],\n    "required_entity_count": 0,',
          )
          replace_once(
              '**`missing_entities`が1件でもあればmerge不可。** 現在のvalidatorがこの配列を機械検査しない場合でも、PRレビュー上のblocking条件として扱う。',
              '**`missing_entities`または`unadjudicated_source_mentions`が1件でもあればmerge不可。** 現在のvalidatorがこれらを機械検査しない場合でも、PRレビュー上のblocking条件として扱う。件数だけでなく、どの一次資料のどの主体言及をどのcanonical entityへ対応付けたか追跡可能にする。',
          )

          replace_once(
              '5. **表示外だが現況に関係する新規機関**：今回の探索で新規発見した機関。表示採用しない場合もNOT_APPLICABLE/UNKNOWN等の裁定を残す。',
              '5. **表示外だが現況に関係する新規機関**：今回の探索で新規発見した機関。表示採用しない場合もNOT_APPLICABLE/UNKNOWN等の裁定を残す。\n6. **一次資料中の支援主体言及集合（source_mentions）**：上記1〜5の探索で開いた公式Web/PDF/JSON本文、派遣元自治体公式ページ、全国組織公式ページ、政府横断資料の本文中に、活動主体・派遣主体・実施主体・共同主体・地方支分部局として登場した全機関。既存recordに紐づかない主体も含める。',
          )
          replace_once(
              'officially_discovered_entities ⊆ required_entities\n```',
              'officially_discovered_entities ⊆ required_entities\nsource_mentions ⊆ required_entities\n```\n\nさらに、次の閉包条件を必須とする。\n\n```text\nsource_mentions - adjudicated_entities = ∅\n```\n\nここで`source_mentions`は検索結果タイトルではなく、実際に確認した一次資料本文・表・別紙・派遣元公式ページ等に登場した主体を指す。',
          )

          replace_once(
              '逆に、前回存在した機関を今回の資料で見つけられなくても監査対象から削除しない。終了を確認できなければUNKNOWNとする。\n\n### 8.1 対口支援・他自治体支援の全件再監査（必須）',
              '''逆に、前回存在した機関を今回の資料で見つけられなくても監査対象から削除しない。終了を確認できなければUNKNOWNとする。

#### 8.0.5 Source Mention Closure（一次資料中の支援主体言及の閉包）

機関漏れ防止のため、探索は最低3パスで行う。

1. **Inventory Pass**：固定最低確認先、現行ダッシュボード、前回municipal/national監査、直前UPDATE_LEDGERから既知entityを列挙する。
2. **Cross-cutting Discovery Pass**：内閣府、熊本県、各省庁災害ページ、政府横断資料、対口支援名簿等から新規entityを発見する。
3. **Source Mention Closure Pass**：1・2で採用した各一次資料を本文まで読み、そこに登場する派遣元自治体、地方支分部局、全国組織、職能団体、共同実施主体を逆引きし、未裁定主体がなくなるまでrequired_entitiesへ追加して再探索する。

一つの中央省庁ページに全国の現況総数がなくても、派遣元自治体・地方支分部局・全国組織の公式ページで基準日時点の活動継続を確認できる場合がある。この場合、**活動状態のCURRENT確認**には使えるが、旧来の全国複合総数をCURRENTへ推定更新してはならない。

各source_idについて最低限、次を確認する。

- 公表主体は誰か。
- 本文中に別の活動主体・派遣主体・共同主体が出ていないか。
- その主体がrequired_entitiesへ入っているか。
- その主体にCURRENT / HISTORICAL / PLANNED / UNKNOWN / CONFLICT / NOT_APPLICABLEの裁定があるか。
- 表示しない主体でも、監査上の除外理由が残っているか。

完了条件は`unadjudicated_source_mentions = []`である。単に`missing_entities = []`でも、一次資料本文に未登録主体が残っていればmerge不可。

### 8.1 対口支援・他自治体支援の全件再監査（必須）''',
          )

          replace_once(
              '- 厚労省の全国保健医療福祉、警察庁の活動、総務省の財政支援等、値が取得しにくい機関も監査対象から外さずUNKNOWNとして残す。\n\n```sh\nnpm run sync:national-support',
              '''- 厚労省の全国保健医療福祉、警察庁の活動、総務省の財政支援等、値が取得しにくい機関も監査対象から外さずUNKNOWNとして残す。

#### 8.2.1 record状態と部分指標・集計値の分離

national支援は「活動しているか」と「全国同一定義の総数が確定しているか」を別軸で裁定する。

例：

- 緊急消防援助隊の現行派遣を派遣元公式情報で確認 → `record.state = CURRENT`。一方、消防・警察・自衛隊・航空を束ねた旧定義の全国総数が確定できない場合、その**複合総数だけUNKNOWN**として`accepted_unresolved`へ残す。
- 保健医療福祉支援の現行派遣例を一次確認 → 活動recordはCURRENT。ただしDHEAT / DMAT / DPAT / 日赤等を束ねた同一定義全国総数が不明なら集計値はUNKNOWN。
- 普通交付税の繰上げ交付「決定」は一次確認できても、現金交付「完了」が一次確認できなければ、決定済みと実施完了UNKNOWNを分離する。
- プッシュ型物資の過去の支援強化は確認できても、reference_at時点の同一スキーム継続を直接確認できなければrecordはUNKNOWNのままにする。

`accepted_unresolved`はentity未監査の逃げ道ではない。entity自体は必ず裁定し、未確定なのが活動状態か、数量・全国集計・実施完了等の部分指標かを明示する。

```sh
npm run sync:national-support''',
          )

          replace_once(
              '「前回と同じ機関しかなかった」場合も`newly_discovered_entities: 0`と明記し、探索を省略して0とみなさない。\n\n## 9. 現在避難所 candidateフロー',
              '''「前回と同じ機関しかなかった」場合も`newly_discovered_entities: 0`と明記し、探索を省略して0とみなさない。

### 8.4 同一reference_atの追加再裁定

同じ基準時点について後から追加一次情報を発見しUNKNOWN等を再裁定する場合、これは新しいtimepoint refreshではなく**same-reference re-adjudication**として扱う。

必須ルール：

- `reference_at`は元の基準日時を維持する。追加調査時刻は`rechecked_at`または同等フィールドで別管理する。
- 「全件再監査済み」という元の監査証跡を、追加再監査の文言だけで上書きしない。表示上も必要に応じて「全件再監査基準＋追加再監査」と併記する。
- 追加一次情報でrecord状態を変更するときも、旧snapshot・旧source_as_ofをHISTORICAL等として保持し、過去値を消さない。
- 同じreference_atでも、追加一次情報でrequired_entities / source_mentionsが増えた場合はInstitution Coverage GateとSource Mention Closureを再実施する。
- sync script、validator、Release Gate自己テスト、表示HTML、post-deploy smokeの固定文言・期待値が同じ事実を参照するよう同一PRで更新する。
- 一時migrationやbot生成commitが入った後は、そのcommitを含む**最終head SHA**に対して通常CIを再実行する。古いheadのPASSを流用しない。
- 一時workflow・migration・markerは最終PR差分から除去し、`expected_changed_files`を最終実差分と一致させてからRelease Gateを通す。

## 9. 現在避難所 candidateフロー''',
          )

          replace_once(
              '14. UNKNOWNの旧snapshotをcurrent表示 → FAIL\n\n機関カバレッジ集合差分をvalidatorへ実装した場合は、`missing_entities > 0 → FAIL`の自己テストも追加する。未実装の間も文書運用上はmerge blockerとする。',
              '14. UNKNOWNの旧snapshotをcurrent表示 → FAIL\n15. `missing_entities > 0` → FAIL\n16. `unadjudicated_source_mentions > 0` → FAIL\n17. same-reference再裁定で元の全件再監査証跡が消失 → FAIL\n18. record CURRENTと部分指標UNKNOWNの区別が失われ、旧全国総数等をCURRENT表示 → FAIL\n\n機関カバレッジ集合差分とSource Mention Closureは可能な限りvalidatorへ実装する。未実装の間も`missing_entities`または`unadjudicated_source_mentions`が1件でもあれば文書運用上のmerge blockerとする。',
          )

          replace_once(
              'データ変更があるのに台帳がない場合はFAIL。',
              'データ変更があるのに台帳がない場合はFAIL。\n\n**CIのPASSはcommit SHA単位で扱う。** bot commit、一時migrationの正式生成物commit、cleanup commit、台帳修正commit等でPR headが動いた場合、それ以前のCI PASSは最終判定に使わず、最終headで通常CIを再実行する。最終CI前に一時workflow / migration / markerを除去し、`expected_changed_files`を実差分へ確定させる。',
          )

          replace_once(
              'CI成功後にhead SHAを固定してmergeする。headが動いた場合は再確認する。',
              'CI成功後にhead SHAを固定してmergeする。headが動いた場合は再確認する。**mergeに使うhead SHAと、通常CI PASSを得たhead SHAは同一でなければならない。** 生成commit・cleanup・台帳調整後は必ずfinal-head CIを取り直す。',
          )

          replace_once(
              '`.github/workflows/post-deploy-pages-smoke.yml`でPages上の主要ファイルとmerged mainの一致、現況確認時刻、release_id、Desktop/Mobile runtime、ビュー、page/console error、重要national表示を自動検査する。workflow PASSを公開後QAの機械的証跡とし、最終目視は補完する。',
              '`.github/workflows/post-deploy-pages-smoke.yml`でPages上の主要ファイルとmerged mainの一致、現況確認時刻、release_id、Desktop/Mobile runtime、ビュー、page/console error、重要national表示を自動検査する。workflow PASSを公開後QAの機械的証跡とし、最終目視は補完する。\n\n表示文言・監査ラベル・重要national状態を変更するPRでは、**post-deploy smokeの固定期待値もmerge前に同じPRで更新する。** 公開後にsmokeの旧文言が原因で誤FAILする状態を残さない。公開後QAはまずHTMLと主要assetのmerged mainとのバイト一致を確認し、その後Desktop/Mobileブラウザ実行を行う。非表示詳細を検査する場合は`body.innerText`だけに依存せず、対象DOMの`textContent`、明示selector、または`document.documentElement.innerHTML`等、表示仕様に合う検査方法を使う。',
          )

          replace_once(
              '- [ ] 最新政府・熊本県等の横断資料から新規機関を探索\n- [ ] 新規機関を`newly_discovered_entities`へ記録',
              '- [ ] 最新政府・熊本県等の横断資料から新規機関を探索\n- [ ] 採用した全一次資料本文・表・別紙から支援主体言及`source_mentions`を抽出\n- [ ] 各source_idから公表主体・派遣主体・共同主体を逆引きしrequired_entitiesへ反映\n- [ ] 新規機関を`newly_discovered_entities`へ記録',
          )
          replace_once(
              '- [ ] `missing_entities = []`\n- [ ] required_entities件数 = adjudicated_entities件数',
              '- [ ] `missing_entities = []`\n- [ ] `unadjudicated_source_mentions = []`\n- [ ] required_entities件数 = adjudicated_entities件数\n- [ ] record状態と部分指標・全国集計UNKNOWNを分離して裁定',
          )
          replace_once(
              '- [ ] 国・関係機関・全国組織を全件再監査\n- [ ] `national-support-audit.json`の`reference_at` = `PAGE_RECHECK_META.checkedAt`',
              '- [ ] 国・関係機関・全国組織を全件再監査\n- [ ] same-reference追加再裁定の場合、reference_at維持・rechecked_at記録・元の全件再監査証跡維持\n- [ ] `national-support-audit.json`の`reference_at` = `PAGE_RECHECK_META.checkedAt`',
          )
          replace_once(
              '- [ ] PR CI PASS\n- [ ] 一時workflow / migration / fixerがPR差分に残っていない',
              '- [ ] PR CI PASS\n- [ ] merge対象のfinal head SHAとCI PASS対象SHAが同一\n- [ ] bot生成・cleanup・台帳調整後にfinal-head CIを再実行済み\n- [ ] 一時workflow / migration / fixer / markerがPR差分に残っていない',
          )
          replace_once(
              '- [ ] Pages実配信確認\n- [ ] Desktop/Mobile・6ビュー確認',
              '- [ ] Pages実配信HTML・主要assetがmerged mainとバイト一致\n- [ ] 表示文言変更時にpost-deploy smoke期待値を同一PRで同期\n- [ ] post-deploy Pages smoke PASS\n- [ ] Desktop/Mobile・6ビュー確認',
          )

          path.write_text(text, encoding='utf-8')
          print('updated to v2.5')
          PY
      - name: Commit documentation update
        shell: bash
        run: |
          set -Eeuo pipefail
          if git diff --quiet -- '時点修正更新オペレーション.md'; then
            echo 'no changes'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -- '時点修正更新オペレーション.md'
          git commit -m 'docs: strengthen exhaustive timepoint re-audit workflow'
          git push origin HEAD:${{ github.event.pull_request.head.ref }}
